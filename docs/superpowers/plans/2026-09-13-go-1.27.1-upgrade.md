# Go 1.27.1 安全升级与构建对齐

用户已批准本计划，仅实施Go工具链升级；第三方业务依赖、UI对比度与Shell存量问题不在修复范围。

## 固定选择

- Go 1.27.1；go.mod及WSL /usr/local/go与所有Go构建镜像对齐，不升级业务依赖，不改变go.sum。
- 官方镜像docker.io/library/golang:1.27.1@sha256:f44f6e88636cfb311f9ebace870ded69d943f227bb3cb27d32ffd84ea18c43ea。
- Go Linux amd64归档SHA256：63d339f0da5ab53635a56f2490a7984dfe12dfcff22ad749f63edaf590168445。
- golangci-lint 2.13.2；govulncheck保持1.7.0并用Go1.27.1重编译；gopls0.22.0及其余生成工具现有版本重编译。用户追加批准mockery从3.6.1升级至3.8.0以解决Go1.27类型加载故障。
- MinIO源码提交不变，默认镜像标签改为athena-minio:9e49d5e7a648-go1.27.1和athena-minio-mc:7394ce0dd2a8-go1.27.1。

## 实施与验证

- [x] 对齐go.mod、五个Go Docker构建阶段及所有MinIO默认标签，更新安装、工具链、运行和镜像文档。
- [x] 安装主机Go1.27.1，升级lint工具；govulncheck安装/就绪检查同时验证工具版本和编译Go版本，测试旧编译器重建、正确版本跳过、缺失和加载错误。
- [x] go test -mod=readonly ./...；构建主服务、Gateway和两个索引器；lint配置验证及实际分析，只修复升级兼容问题，保留存量诊断。
- [x] 隔离副本执行完整现有生成流程、核对差异，不将无关生成结果带回交付。
- [x] 构建主服务、两索引器、MinIO server和mc五个镜像；确认新Go产物，验证MinIO运行及初始化。
- [x] 漏洞复扫按ID、模块、调用路径对照，不预设9项阈值；要求标准库漏洞路径不再出现，第三方残留仍返回失败并记录。
- [x] 完整隔离浏览器验收；在原项目同步增量后准备真实开发环境，验证会员/管理员bootstrap和smoke，保留服务会话日志及停止说明。
- [x] 审查增量，保留其他任务改动；必要验收完成后从原仓库发送一次中文完成邮件。

## 边界

不改变业务API、数据模型、Node/Yarn或业务依赖；不reset数据、不终止归属不明进程、不部署远端、不提交推送。必要业务依赖升级超出范围时保留证据报告阻塞。无关lint风格问题仅记录，不全仓格式化或禁用规则。

## 执行状态

验收详情见 [Go 升级验收记录](../../testing/go-1.27.1-upgrade.md)。用户已明确批准mockery3.8.0；交付安装器及原项目二进制均已升级，并通过最终安装器的隔离完整代码生成、mock包测试和差异核对。全部必要验收已完成，原项目开发环境保留运行；独立审查无阻断问题，已从原仓库发送一次完成邮件，SMTP 接受、Make 退出0。业务依赖、go.sum及无关生成源码保持不变。
