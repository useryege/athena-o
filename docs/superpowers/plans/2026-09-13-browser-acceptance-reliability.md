# 浏览器验收可靠性修复计划

状态：实现与验证已完成；完成通知结果见交付报告。

目标：修复 SIGHUP 清理、bootstrap 恢复误报与 Chrome 相对路径。沿用统一入口和 fixture/live/smoke 分层，不改变产品 API 或认证行为。

## 已确认边界

- 仅提供挂断清理与人工恢复指引，不做自动失效锁或资源回收。
- 保留 Procfile、Docker、Node 工具链及其他现有修改；不提交、不推送。
- 在当前未提交快照的隔离工作区实施，完成后仅同步本次增量。

## 实施任务

- [x] 运行器：统一处理 SIGHUP/SIGINT/SIGTERM，退出 129/130/143，覆盖预检、取锁、构建、测试等待；重复信号不重复清理，保留原失败与清理错误。
- [x] 路径：Chrome 路径相对于启动目录转为绝对路径，校验、版本探测、子进程与报告一致；显式配置无效仍失败。
- [x] 冒烟：导航起 15 秒内等待应用自身 bootstrap 重试与对应页面就绪，保留中间失败诊断并在资源稳定后复查；不补发请求或刷新页面。
- [x] 回归：先写失败用例，再实现修复；覆盖挂断各阶段、重复信号、清理失败、无关进程、再次运行、Chrome 路径，以及双 realm/双 prefix 的 HTTP 与 JSON 恢复、持续失败和身份边界。
- [x] 文档：同步维护运行说明和验收规格，增加按运行归属确认的人工恢复步骤。
- [x] 验证：新增/原有自测、E2E 类型检查、根路径和 /athena 完整 fixture/live、资源归属与清理核验；可访问的已有开发环境执行 smoke，否则如实记录阻塞。
- [x] 完成：独立代码审查、同步增量、确认其他修改保持原样，从原仓库根按默认配置发送一次中文完成邮件。

## 规格

沿用并更新 [浏览器验收设计](../specs/2026-09-13-athena-browser-acceptance-design.md)。运行与恢复步骤维护在 [本地运行说明](../../developer-guide/running-locally.md)。

## 本次验证记录

- 运行器 33/33、受控 smoke 22/22、manual context trace 2/2，共 57/57 通过，零跳过；E2E TypeScript 与脚本语法检查通过。
- 根路径 fixture 33/33、live 10/10；/athena fixture 33/33、live 10/10，共 86/86，零跳过、零 flaky。
- 隔离运行 2026-09-13T04-55-12-037Z-c2941497：两个数据库不同，清理成功，本轮容器和仓库锁无残留；原有 7 个运行容器 ID/名称保持一致。
- 实际开发环境 smoke 运行 2026-09-13T04-55-11-642Z-dd976c0a：localhost:4000 两入口连接拒绝，作为环境阻塞保留；未启停开发服务。
- 独立审查发现近截止时间的1ms复查误报，补充 near-deadline 和在飞 bootstrap 用例确认 red，再修复并得到 green 2/2；定向复审通过，无未解决 P1/P2。
- 完整日志和报告保留在隔离工作区的 .superpowers/sdd/browser-acceptance-reliability/ 与 .tmp/athena-ui-acceptance/；交付摘要同步到原工作区 .tmp/browser-acceptance-reliability-2026-09-13.md。

- 原工作区同步后聚焦检查 12/12、两模式预检及 E2E 类型检查通过；完成邮件已从原仓库根目录发送，SMTP 第一次尝试接受。
