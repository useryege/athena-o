# <能力名称>需求目标

> 需求状态：讨论中
>
> 关联技术设计：未创建

复制本模板后删除所有说明性占位内容。本文用于长期维护业务目标和可观察行为，不预先规定后端实现。状态描述业务决定的确定程度，不作为审批开关；按实际情况填写，并可补充相关 Superpowers 规格链接。新任务按 [Superpowers 开发工作流](../developer-guide/superpowers-development.md) 开展，复杂任务规格默认保存在 `docs/superpowers/specs/`。

## 背景与问题

说明当前场景、需要解决的问题以及为什么现在需要处理。

## 目标

- 列出完成后必须达成的业务结果。

## 非目标

- 列出本次明确不解决的事项，防止范围被默认扩大。

## 角色与权限

说明涉及的角色、各自可执行的动作、可查看的数据及禁止边界。

## 业务术语

| 术语 | 定义 |
| --- | --- |
| `<术语>` | `<唯一、可验证的业务含义>` |

## 业务规则与不变量

- 写出任何实现都必须保持的规则。

## 主流程

按业务发生顺序描述正常路径，以及每一步对用户或外部系统可见的结果。

## 状态转换

列出业务状态、允许的转换、触发条件和禁止转换；没有状态模型时写“不适用”。

## 异常与边界场景

说明无权限、重复请求、输入缺失、外部依赖失败、部分结果和范围边界下的目标行为。

## 输入与可见输出

描述业务输入、对调用者或用户可见的输出和错误语义。字段级接口契约留给技术设计。

## 验收场景

- 给出能够判断需求是否满足的具体场景与结果。

## 已确认决定

- 仅保留当前仍有效、会约束技术设计或实现的决定。

## 待确认问题

- 列出尚未决定的问题及其可能影响，区分已有决定和拟议变化，不将未决事项写成既定业务规则。
