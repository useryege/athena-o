# <能力名称>后端技术设计

> 设计状态：设计中
>
> 关联需求：[<相关需求名称>](../../requirements/<业务域>/<文档>.md)

复制本模板后删除所有说明性占位内容，并按文件所在目录调整相对链接。本文用于长期架构知识维护，按实际情况填写描述性状态，并可补充相关 Superpowers 规格与计划链接；没有独立需求文档时如实说明。新任务按 [Superpowers 开发工作流](../developer-guide/superpowers-development.md) 开展，复杂任务规格与实现计划默认保存在 `docs/superpowers/specs/` 和 `docs/superpowers/plans/`，不以本模板或状态字段设置额外审批。

## 需求覆盖

逐项列出本设计覆盖的需求规则、验收场景和明确不覆盖的范围，确保每个技术决定都有需求来源。

| 需求条目 | 设计落点 | 说明 |
| --- | --- | --- |
| `<需求规则或验收场景>` | `<本文章节>` | `<覆盖方式或边界>` |

## 范围

说明本能力负责什么、与哪些相邻能力交互，以及本次设计不承担什么。

## 现状与目标差距

基于代码和已有 `已实现` 设计说明当前行为，再列出达到目标所需的差距。进入 `已实现` 后只保留仍然真实的现状，并明确不存在未实现差距。

## 关键决定

记录会约束实现的重要方案与取舍，包括采用原因和被排除方案的必要结论；不保留讨论流水账。

## 组件与职责

说明组件边界、依赖方向、调用关系和各自唯一职责。只有在关系较复杂时添加小型图示。

涉及新业务服务或服务边界改造时，同时记录服务所有者（owner）、独立进程和部署、独立构建/启动/测试/停止入口及必要依赖，并引用适用的[服务开发规范](../developer-guide/service-development-standards.md#sds-r1)规则。说明构建和局部运行怎样以目标服务及最小依赖正向选择，以及完整栈如何显式选择；纯内部库不适用时说明其仅在何处被调用，避免重复填写同一边界事实。

## 接口与数据契约

定义内部或外部接口、消息、事件、字段语义、错误语义、兼容边界和调用约束。没有契约变化时写“不适用”。

## 数据模型与持久化

说明实体、所有权、生命周期、唯一性、索引、状态转换和持久化边界；没有持久化状态时写“不适用”。

## 运行流程

按顺序描述启动、主路径、异常路径、停止，以及跨组件的数据流和状态提交点。

## 事务、并发与幂等

说明原子边界、锁或版本策略、并发顺序、重复请求处理、任务领取和重试幂等性；不适用时说明原因。

涉及共享数据库、撤权或跨服务协调时，记录 transaction 和连接池的 owner、受控 adapter 的借用关系，以及为何不跨 RPC 传递 transaction；原子撤权不能因拆分随意改成异步。参见[服务开发规范 SDS-R6](../developer-guide/service-development-standards.md#sds-r6)。

## 失败处理与恢复

说明依赖失败、部分完成、重试、取消、重启恢复、人工介入和永久失败语义。

## 配置、安全与权限

列出配置来源、默认值、校验、凭据边界、鉴权与授权要求，以及敏感操作的安全约束。不得复制密钥。

服务边界变更还应说明 API 承担公共协议、身份/session 与聚合/facade，且不持有/借用服务 runtime 或启动其后台任务；服务自有配置、仅校验自身和声明依赖配置的范围、用户请求的可信用户 identity、后台/系统请求的可信服务身份、两类调用的内部鉴权与业务权威权限检查、RPC deadline，以及配置/依赖失败对 API 和相邻服务的影响；不适用时说明原因。参见[服务开发规范 SDS-R2、SDS-R4](../developer-guide/service-development-standards.md#sds-r2)。

## 可观测性与运维

定义关键日志、指标、追踪、健康与就绪语义、状态查询和诊断所需字段。

局部运行时必须说明其拥有的资源、局部停止和清理边界，且不得把全局 reset 当作局部回收手段。参见[服务开发规范 SDS-R3、SDS-R5](../developer-guide/service-development-standards.md#sds-r3)。

## 源码影响

列出预计新增、修改、替换或删除的源码位置和关键符号。设计阶段可以标记预计路径，实现完成后必须更新为实际位置。

| 关注点 | 源码位置 | 关键符号 | 动作 |
| --- | --- | --- | --- |
| `<入口或边界>` | `<path/to/file>` | `<SymbolName>` | `<新增/修改/替换/删除>` |

## 旧实现清理

列出应直接替换或删除的旧路径、字段、配置和死代码。ATHENA 不为历史行为保留兼容层。

## 验证方式

记录用于证明需求覆盖和实现行为的验证方式与实际结果。实现遵循 Superpowers 的 TDD 与完成前验证技能；验证范围和执行细节保存在对应任务计划中，本节保留理解当前架构所需的证据。

服务或边界变更应列出独立入口、授权、事务、故障和停止语义的实际证据；独立进程和跨服务契约还要列出构建/部署、最小依赖选择、RPC deadline 和故障隔离证据。仅文档变更可记录链接与静态检查。参见[服务开发规范 SDS-R8](../developer-guide/service-development-standards.md#sds-r8)。

## 待确认问题

列出尚未决定的问题及其影响，区分当前有效方案与拟议变化，不将未决事项写成既定架构。

## 文档维护检查

- [ ] 已记录业务规则在设计和实现中的落点，以及仍未完成的差距。
- [ ] 组件、接口、数据、事务、并发和失败语义与实现一致。
- [ ] 配置、安全、权限、可观测性和运维入口与实现一致。
- [ ] 当前实现、目标方案和未决问题已清楚区分。
- [ ] 旧实现已按设计清理，没有无需求依据的兼容路径。
- [ ] 源码链接和关键符号可以解析。
- [ ] 文档状态如实描述方案和实现情况，并已登记到[设计索引](README.md)。
