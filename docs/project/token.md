# Token 两板块目标设计

> 文档状态：目标设计草案，尚未实现。本文件记录已明确的业务目标与开发阶段范围，并将待确认建议、待细化规则和未采用的调研方案分别列出；不代表当前程序已经具备完整目标能力。

本文用于需求讨论和开发设计。具体技术方案与代码开发在对应业务规则和文档对齐后推进，整体目标尚未全部实现。

流程图按“一图一文件”独立保存在本目录，入口见 [流程图索引](README.md#流程图)。本文件保留业务规则，各节链接到对应流程图；后续规则调整时同步维护相关图示与说明。

## 业务目标

Token 的核心目的是发现区块链上的新代币项目，从合约部署后开始捕捉和研究项目信息，判断项目是否值得关注、是否具备买入价值。

系统争取在项目尚未开启交易时完成研究和筛选，持续关注筛选出的项目，并在其开启交易且满足买入条件时及时买入。尽早参与是寻找盈利机会的业务动机，不构成盈利保证。

## 已明确的实施顺序与前端范围

Token 先完成后端业务逻辑，再重新设计和实现前端 UI。现阶段删除原有 Token 项目、源码、黑名单、节点状态、链处理及采集任务页面，以及它们的路由、前端服务、专用组件、样式和资源；文中研究资料的展示要求作为后续前端设计依据，不要求保留原有页面。

成员端保留 `Token & Risk` 分组。`Token` 父菜单保留原名称、图标及 Token 读取权限可见性，显示为禁用项，无子菜单、跳转路径或占位页面；独立的 `Wallets` 保留。桌面侧栏与移动端抽屉采用相同结构，访问 `/token` 或 `/token/*` 使用现有通用 404 页面。成员端默认首页继续为 Active 账户的个人资料页或 Pending 账户的权限页，不再将 Token 作为获得权限后的自动跳转目标。

管理员权限编辑器及账户中心不展示或编辑 Token 权限项，相应展示统计也排除 Token。完整九项权限模型、后端鉴权及已有 Token 授权继续保留；编辑其他权限时必须保留原有 Token 值，不得遗漏或重置。仅持有 Token 授权的账户仍按原规则判定为 Active。

本阶段不删除 Token HTTP/gRPC 接口、采集服务、数据库或 API 文档。管理员通用服务状态中的 Token API 健康信息、Etherscan 管理、独立钱包、Worm Trading 和账户 API Key 功能保留。当前实现说明随界面清理同步更新，后续业务逻辑开发继续遵循先对齐需求文档、再修改代码的顺序。

## 项目的典型阶段

| 阶段 | 项目侧的活动 | 系统关注的业务问题 |
| --- | --- | --- |
| 合约部署 | 发起者发布合约和代币 | 如何尽早发现项目并开始收集信息？ |
| 交易开放前的准备 | 发起者准备交易条件，包括添加流动性等操作 | 项目是否值得关注，能否在开放交易前完成筛选？ |
| 开放交易 | 项目开始允许交易 | 关注项目是否已经开放交易，当前是否满足买入条件？ |
| 普通交易者参与 | 链上交易者开始买卖项目代币 | 如何在符合条件的前提下及时参与？ |

这些阶段用于说明业务目标，不是已确定的程序状态枚举，也不假设每个项目都严格按此顺序经历独立的阶段。添加流动性与开放交易分别描述：添加底池本身不作为已经开放交易的充分判据，具体判定规则仍待设计。项目在研究完成前就开放交易的情况也需要单独明确处理方式。

## 已明确的两个业务板块

| 板块 | 核心问题 | 输入 | 主要工作 | 输出 |
| --- | --- | --- | --- | --- |
| 项目发现与研究 | 这个新项目是否符合研究筛选规则、值得关注？ | 开发阶段范围内的顶层部署候选及可获得的项目资料 | 从部署后开始发现并识别 Token，采集资料、开展研究，按规则筛选项目并保存结论与原因 | 符合规则的关注项目及研究结论与依据；被过滤项目的未通过规则、原因和依据记录，供后续人工排查 |
| 交易监控与执行 | 这个关注项目现在是否满足买入条件？ | 第一板块交接的关注项目及研究信息 | 持续检测是否开放交易，在开放交易且满足条件后及时买入 | 买入执行结果，具体结果分类与处理规则待细化 |

“值得关注”是第一板块按研究筛选规则得出的结论；不符合规则的项目被过滤，并保留具体原因和依据供后续人工排查。“满足买入条件”是第二板块执行买入前的条件判断，进入关注范围不直接等于执行买入。

在交易开放前完成研究是业务时效目标。第一板块的研究与交接不以检查底池或证明尚未添加流动性为前提，实际交易状态和买入条件由第二板块判断。

图示见 [Token 整体业务流程](token-overall-flow.md)，表达已明确的两板块业务主线，并注明尚未决定的异常分支与执行细节。

两个板块目前是业务职责划分，不预先决定服务数量、部署进程或通信技术。项目身份、研究结论与依据是描述交接所需的概念信息；本轮不定义字段结构、API 或事件协议。

## 已明确的开发阶段发现范围

当前开发阶段仅关注区块内满足 `transaction.To() == nil` 的顶层合约创建交易，使用交易发送者和 nonce 推导候选合约地址。该范围沿用当前扫描入口，便于先推进项目研究和交易监控的业务设计。

- 工厂合约内部通过 `CREATE` 或 `CREATE2` 创建的合约暂不纳入发现范围；即使内部创建发生在顶层部署交易中，也只跟踪顶层候选合约。
- 项目发现暂不接入 Bitquery 或 Allium，也不在本阶段增加内部调用追踪或工厂事件扫描来扩展创建路径覆盖。
- `transaction.To() == nil` 只用于确定部署候选，不代表候选一定是代币或项目已经值得关注。发现地址后，由聚合合约内部先检查代码是否存在，再按下文已明确的条件识别 Token；不设置独立的部署结果确认环节，也不通过交易回执判断交易是否成功。项目研究筛选标准后续细化。

因此，本阶段的发现结果不代表区块内创建的全部合约。内部创建路径的扩展可在后续重新讨论，不作为当前开发的前置条件。支持链范围仍需另外明确；DEX 和交易池覆盖属于后续交易监控设计，不作为顶层项目发现与研究的前提。

## 已明确的区块扫描调度（目标规则、尚未实现）

扫描采用主动查询最新区块、按高度连续逐块处理的循环。每轮最多处理 100 个区块，轮末重新查询最新高度；追平后等待 1 秒再查询。同一条链按顺序追赶已有进度之后的区块，历史补扫和新区块扫描沿用同一条连续扫描流程。

### 启动与扫描进度

- 数据库已有该链的扫描进度时，从已保存位置继续，不重新按三天回看窗口初始化。
- 数据库没有该链的扫描进度时，读取当前最新区块，定位向前回看三天所对应的起始区块，从该区块开始扫描。三天是时间范围，不固定换算成某个区块数量；具体定位方式后续设计。
- 本文将扫描游标 `C` 定义为最后一个已成功处理并保存进度的区块高度，下一块为 `C + 1`。首次确定起始区块后，将初始扫描位置设为该区块之前，以包含起始区块。
- 每个区块成功处理后保存进度；没有部署候选的区块也需要记录扫描完成。每轮 100 块是调度上限，不是要求整批完成后才保存进度。

### 每轮执行规则

1. 查询本轮最新区块高度 `H`。
2. 若 `H > C`，确定本轮范围为 `C + 1` 到 `min(C + 100, H)`，两端均包含；本轮上限确定后不随新块产生而延长。
3. 在本轮范围内逐块处理，成功处理一块后保存该块进度，再处理下一块。区块处理失败时，每次都通过通知组件通知管理员，然后重试同一区块；重试次数不设上限，持续失败就持续通知，直到该区块成功或程序停止。失败或尚未完成的区块不越过、不推进为已完成。
4. 本轮完成后立即回到第 1 步，重新查询最新高度，再计算下一轮范围；仍有积压时不额外等待 1 秒。
5. 若 `H == C`，说明按当前高度已追平，等待 1 秒后回到第 1 步。等待应允许程序停止时退出。
6. 若 `H < C`，进入异常分支，保留已保存的扫描进度，通过通知组件向管理员发送异常通知，交由管理员人工处理。该分支不按正常追平处理，也不自动回退或重新初始化扫描进度。

例如，已成功处理到 1800，最新高度为 2000，下一轮处理 **1801–1900，共 100 块**；完成后重新查询最新高度，再从 1901 继续。另一个例子中，若游标为 1900 且最新高度为 1950，则这一轮只处理 1901–1950，共 50 块。已处理到 2100 且最新高度也为 2100 时，等待 1 秒后重新查询。

图示见 [Token 区块扫描流程](token-block-scan-flow.md)，包含启动、批次、进度保存、追平等待、失败通知与无限重试，以及 `H < C` 人工处理分支。

本节确定正常扫描的启动、顺序、批次和等待规则；不采用新区块订阅作为触发方式，也不增加实时优先与历史补扫的独立处理流程。逐区块处理属于扫描调度约束，单块完成需要取得并保存哪些发现结果、是否等待候选验证、与后续项目研究如何衔接，仍需明确。

区块处理失败的每一次尝试都产生一次管理员通知，不因同一区块或同类错误重复发生而去重、合并或抑制，也不设置只通知首次失败的规则。每轮最多 100 个区块限制的是不同区块的处理范围，不限制同一区块的重试次数；失败块成功后，才继续本轮的下一块。重试间隔及通知提交失败时的处理方式后续细化。

区块处理失败后的持续通知与重试，以及 `H < C` 时通知管理员人工处理，均复用仓库的 [系统通知组件](../design/notifications/system-notification-operations.md)。`H < C` 的人工处理分支独立于区块失败重试循环；该分支的重复通知控制、异常期间扫描控制及人工恢复方式仍待细化。具体告警内容、通知接入方式与接收配置后续设计；本轮不改变通知组件自身的投递和重试机制。

上述高度比较不覆盖同高度区块被替换等全部链重组情况。区块哈希核验、其他重组情形的处理及其对候选、研究和关注结果的影响，继续留待设计。本轮只更新目标文档，尚未接入异常通知或修改扫描代码，现状见 [Token 链扫描与项目初始化](../design/token-intelligence/chain-processor.md)。

## 已明确的 Token 识别与逐项诊断

图示见 [Token 候选发现与识别流程](token-discovery-identification-flow.md)，从顶层创建交易推导地址开始，展示代码检查、六项检测、识别结论与原因的持久保存。

### 合约内部先检查目标地址代码（待实现）

Token 识别入口在聚合合约内部先检查目标地址的 `code.length`：

- `code.length == 0`：直接判定为 **不是 Token**，返回并保存原因“地址无合约代码”，不继续执行后续六项调用。
- `code.length > 0`：继续执行下述现有筛选条件。

不单独检查部署交易是否成功，也不把交易回执的 `status` 作为识别前置条件。诊断原因记录查询时的实际事实“地址无合约代码”，不将其直接推断为原始部署交易失败。回执中的转账日志仍可作为初始接收钱包等研究资料的来源，具体研究采集范围按相关章节设计。

### 保留现有六项检测条件

Token 识别保留当前全部筛选条件，不放宽元数据、精度或供应量要求，也不以其他识别建议替换现有规则。以下五组业务条件对应六个独立调用。

**识别结论只有两种：是 Token、不是 Token。** 目标地址有代码且六项全部通过时判定为 Token；目标地址无代码或任意一项未通过时判定为不是 Token，并返回对应原因。不设置“暂时无法判断”“待进一步识别”等第三种识别结果。

| 检测项 | 通过条件 |
| --- | --- |
| `name()` | 能读取且非空 |
| `symbol()` | 能读取且非空 |
| `decimals()` | 能读取且大于 0 |
| `totalSupply()` | 能读取且大于 0 |
| `balanceOf(零地址)` | 能正常返回并按现有要求解码；允许返回 0 |
| `allowance(零地址, 零地址)` | 能正常返回并按现有要求解码；允许返回 0 |

读取和解码要求沿用 [当前 Token 探测逻辑](../../pkg/abi/ATHENA/ATHENA.sol) 的 `_getToken` 及相关 `_safe*` 方法。名称或符号为空、精度或总供应量为 0，均按现有规则判定该项不通过；余额和授权额度不增加大于 0 的要求。这一步记录项目采用的 Token 筛选结果，是否值得关注仍由后续研究判断。

### 返回并保存逐项检测结果（待实现）

本次新增要求是在“是 Token / 不是 Token”的整体识别结论之外，让调用方能够明确知道哪些检测未通过。识别结论、未通过的检测项及对应原因必须随结果返回，并关联到对应链和合约持久保存，供后续人工查询排查。判定为不是 Token 的合约也要保留这份记录，不能仅返回原因或输出日志后丢弃。

- 记录合约内部的代码存在检查结果；目标地址无代码时返回不是 Token 及“地址无合约代码”的原因，不把未执行的六项调用伪造为已经检测失败。
- 目标地址有代码时，分别返回六个调用对应的检测结果；`name` 与 `symbol` 各自记录，不合并成一个无法定位的失败项。
- 对未通过项说明原因，区分调用失败、返回数据无法解码，以及成功读取但值不满足条件。这些情况均判定对应检测项未通过，整体为不是 Token。例如，`balanceOf(零地址)` 调用失败和 `decimals()` 返回 0 是不同原因。
- 单项不通过时，继续完成其余可执行检测，并列出所有已发现的未通过项，避免只保留第一个失败原因。
- 已取得的返回值可作为诊断依据；底层失败原因只能记录实际取得的信息，不能把“调用失败”直接断言为“方法不存在”。
- 若节点请求或外层聚合调用整体失败，按实际情况记录执行错误，不伪造逐项检测结果，也不新增第三种 Token 识别结论。此类执行异常与既定区块失败通知、重试流程的衔接仍待细化。

以下为目标地址有代码时的诊断内容示意，不定义实际返回字段或错误码：

| 检测项 | 检测结果 | 排查信息 |
| --- | --- | --- |
| `name()` | 通过 | 返回非空名称 |
| `symbol()` | 通过 | 返回非空符号 |
| `decimals()` | 不通过 | 调用成功，返回 0，不满足大于 0 的条件 |
| `totalSupply()` | 通过 | 返回值大于 0 |
| `balanceOf(零地址)` | 不通过 | 调用失败，未取得余额结果 |
| `allowance(零地址, 零地址)` | 通过 | 正常返回 0 |

该示例的整体识别结论为 **不是 Token**，未通过项为 `decimals()` 和 `balanceOf(零地址)`，两项原因均应返回并保存，后续人工能够按对应合约查到该结论和原因。具体返回结构、错误码、存储结构与人工查询入口、检测资源限制及异常传播方式后续设计。本轮只记录目标要求；当前筛选条件继续保留，逐项诊断能力尚未实现。

## 已明确的未添加流动性阶段采集范围

图示见 [Token 研究资料整理流程](token-research-materials-flow.md)，单独展开基础信息、源码、源码中的公开资料链接、税率、持币地址和关联钱包资料的依赖及汇集关系；具体源码获取继续见对应独立流程图。

本节针对合约部署后的早期项目发现与研究，以尚未添加流动性的项目为业务场景。第一板块不采集或研究底池信息，也不通过查询底池来验证项目是否属于该阶段。以下为已确定的目标安排，尚未修改当前六源采集代码；底池与交易开放状态的判断交由后续交易监控与执行板块设计。

| 数据类别 | 本阶段安排 | 研究用途与边界 |
| --- | --- | --- |
| 链上状态 `chain_state` | 拆分后仅采集代币基础信息 | 为项目身份和代币基础事实提供依据；本阶段不采集底池地址、创建状态、流动性或 LP 信息 |
| 合约源码 `contract_code_source` | 按字节码哈希复用源码及其 AI 静态质检报告 | 没有源码时采集，没有报告时由 AI 静态审阅并评定风险等级；同一份源码只生成一份共享报告，高风险项目直接排除，不再研究 |
| 源码中的项目公开资料（新增研究资料） | AI 从已取得的源码中识别公开资料链接，重点提取 HTTPS 链接 | 收集官网、X、Telegram、项目文档、GitHub、公开审计报告等多条链接，分类、去重并保留源码出处；同一源码复用已完成提取结果，展示时标注“来源：合约源码”。只提取和展示，不抓取网页、核验官方身份或增加过滤 |
| 合约税率（新增研究资料） | AI 根据源码确定税率读函数及解释方式，Go 读取当前链上值 | 按实际源码区分税率类型与分项，保存原始值、单位或分母、可确定的百分比及来源区块；同源码共享读取方案，实际值逐合约读取。本项仅采集展示，不增加税率阈值或筛选规则 |
| 当前持币地址（新增研究资料） | 后续使用专用 Etherscan PRO API Key 获取持币地址列表、各地址持有量和持币地址总数 | 支持分页，记录查询时间、来源及已取得范围；结合有依据的总供应量计算占比并匹配已有角色。当前免费 Key 继续原有用途，专用 PRO Key 未配置时记录未采集；本项只展示，不增加过滤或自动扩展关联钱包研究 |
| 钱包历史交易 `wallet_normal_transactions` | 原样保留部署前最近最多 300 笔普通交易的查询规则，从 L1 直接关联钱包逐层研究资金来源，最多至 L5 | 各层每个去重钱包分别查询区块 `0` 至 `部署区块 - 1`，倒序取第一页最多 300 笔；分析直接部署的合约及交互代币，补充同一批交易回执与日志，保存历史项目和资金关系依据，并提供分层资金来源图；owner 由 AI 确定读取方式后由 Go 读取，税费接收钱包由 AI 提取源码固定地址或提供方式交由 Go 读取，取得地址后均纳入 L1 |
| 钱包资产 `wallet_asset_state` | 继续采集 L1 直接关联钱包，作为辅助信息，不采集原生币余额 | 读取部署者、所选初始接收钱包、已取得 owner 及税费接收钱包的 WETH/WBNB、USDT 等已追踪代币余额；这些余额用于研究背景，不作为完整钱包估值或项目可靠性的单独判据；L2–L5 新增范围为历史交易、行为研究及资金关系 |
| 模拟结果 `simulation_result` | 暂不作为必采项 | 不作为形成研究结论或交接关注项目的前置条件；是否按需采集、如何解释结果，后续再设计 |
| Ave 数据 `ave` | 本阶段不采集 | 本阶段不调用 Ave 获取市场数据、项目资料或风险标签；后续阶段是否采集及何时触发，另行设计 |

### 链上状态的拆分

链上状态是研究的重要依据，需要将现有 `chain_state` 中的代币基础信息与底池信息拆开，保留前者作为本阶段资料，底池相关内容由后续交易监控与执行板块另行设计。税率作为新增的独立研究资料，按源码确定的读取方式采集，不扩充 Token 识别的六项条件；具体采集任务和数据结构后续设计。

| 数据部分 | 内容 | 归属与采集时机 |
| --- | --- | --- |
| 代币基础信息 | 合约地址、ERC-20 探测结果、名称、符号、精度、总供应量、合约 `code.length`（链上运行时代码的字节数）及对应区块与时间 | 项目发现与研究阶段采集，不依赖项目已经建立交易池或添加流动性 |
| 合约税率 | 源码识别的税率类型与分项、读函数、当前返回值及换算依据 | 项目研究阶段按 AI 确定的方式读取当前合约，保存实际区块与时间，不作为 Token 识别条件 |
| 底池信息 | 候选池地址、池合约是否创建、是否添加流动性，以及池内资产余额、池深、LP 供应与持有情况、锁定情况和流动性估值等 | 本研究阶段全部不采集、不研究；后续由交易监控与执行板块确定所需内容、采集时机及判断规则 |

代币基础信息、字节码信息、合约 owner、需要链上读取的税费接收地址、税率及钱包资产等链上状态，明确以各自采集时的最新区块作为数据源，保存实际读取的区块号及对应时间。不同采集不要求使用同一个区块；同一税率的相关读函数及动态分母如需多次调用，则固定使用该次税率采集的区块。同一数据时点已取得的信息可以复用，旧识别快照不能直接当作本次最新状态。部署资料仍保留实际部署区块，钱包历史仍采集部署前交易；Etherscan 源码查询记录查询时间与资料来源，不视为指定区块的源码快照。直接从源码提取的固定税费接收地址保留源码依据，不记成链上读取结果。

持币地址列表和持币地址总数来自 Etherscan 查询时的当前索引结果，单独记录查询起止时间、接口来源与分页范围；接口未提供对应区块时，不填写推测区块，也不将不同分页或总数响应视为同一区块的一致快照。该时点范围独立于部署区块及关联钱包部署前 300 笔窗口。

代币基础信息需要获取并保存目标代币合约地址在查询区块的 `code.length` 具体数值，以字节为单位，关联对应链、合约、区块与时间。该值描述目标地址上已部署的运行时代码长度，不是源码文本长度或部署交易输入长度。识别入口已有的 `code.length == 0` 检查继续保留；本次补充要求保存实际长度，不能只保留“有无代码”的判断。长度数据先作为基础事实采集，具体研究用途待用户后续明确，本轮不增加长度阈值、评分或筛选规则。该采集与保存要求尚未实现。

研究的启动、完成和关注项目交接均不要求提供底池地址或底池状态快照，也不因没有这些数据就判定项目异常或研究证据不完整。第一板块未采集底池数据，不表示已经确认项目没有底池或流动性。池子存在、已添加流动性、已开放交易和实际可以买入的关系，由第二板块的后续设计明确。

这里先确定数据职责与阶段边界，不预先确定拆分后的任务名称、服务数量、采集频率、接口或数据库结构。当前基础信息、池信息和模拟字段的实现背景见 [ATHENA 聚合查询](../design/token-intelligence/athena-contract.md) 与 [现有采集数据定义](../../internal/token/collection/payload.go)。

### 合约源码按字节码哈希复用

源码采集以目标合约的运行时字节码哈希 `code_hash` 查询数据库。该哈希由链上字节码计算得到，不是对 Etherscan 返回的源码文本计算哈希。对于重复部署且运行时字节码哈希相同的合约，复用此前已获取的源码资料，减少重复 API 请求。

已明确的主流程如下：

1. 取得新合约的运行时字节码哈希。
2. 按该哈希查询数据库中已有的源码资料。
3. 相同哈希已经保存非空源码时，直接关联并复用已有源码，不再为本次源码采集调用 Etherscan API。
4. 相同哈希没有可复用的非空源码时，通过当前合约所属链和地址调用 Etherscan API。此前尚未查询过，以及此前查询过但未取得源码的情况，均进入这条路径；不会因为其他地址曾返回空源码而跳过当前合约的查询。
5. 成功取得源码后，保存源码与字节码哈希的关联，供当前项目研究及后续相同哈希的合约复用。
6. 请求成功但源码为空时，将当前合约标记为“合约未开源”，保存查询结果，结束本次源码采集，不触发请求失败重试。
7. 请求失败时记录实际错误，使用当前合约所属链和地址重试，最多重试三次；重试耗尽后通过系统通知组件通知管理员，将当前合约状态记为“未开源”，同时保留源码请求失败原因与次数，结束本次源码采集。

本流程中的“源码获取成功”表示已经取得非空源码。接口正常响应与成功取得源码需要区分，不能把没有源码内容的响应描述为源码获取成功。Etherscan 的 [源码接口说明](https://docs.etherscan.io/api-reference/endpoint/getsourcecode) 面向已验证合约的源码；链上已有字节码不代表 Etherscan 已有可提供的源码。“合约未开源”作为本业务的状态记录，包含接口成功但源码为空、请求失败且重试耗尽两类原因，分别保存实际原因，并关联查询的链、地址与时间。失败分支的状态不表示已经确认外部平台没有公开源码，也不将该字节码哈希永久标记为不可能取得源码。

本轮将“最多重试三次”按首次请求之外最多再请求三次记录，即一次当前合约源码查询最多四次请求。任一次请求成功后即退出失败重试循环，再按源码是否为空处理；只有请求失败才消耗重试机会。三次重试均失败后，复用 [系统通知组件](../design/notifications/system-notification-operations.md) 通知管理员，说明当前链、合约、失败原因与请求次数，再记录当前合约未开源。正常返回空源码的分支不增加该耗尽通知。

图示见 [Token 合约源码获取流程](token-contract-source-flow.md)，包含按哈希复用、当前合约重新查询、空源码标记、失败重试计数与耗尽通知。

“存在相同哈希记录”和“已经获取到源码”需要区分：只有哈希记录、尚未执行源码查询，不代表已有源码可以复用；此前返回空源码也不作为跳过当前合约查询的依据。重新查询仍以当前合约为对象，不沿用之前未取得源码的地址；取得源码后即可补充该哈希的共享源码资料。

源码请求的有限重试与扫描区块处理失败时的无限重试是不同层次的规则，本轮不改变既定扫描规则。源码重试间隔、通知提交失败时的处理、与研究完成或区块处理的衔接、同一合约日后的定时补采、同一哈希并发请求协调，以及接口和数据库结构仍待设计。通知组件自身的投递与重试机制沿用现有设计。“合约未开源”是否触发项目过滤也仍属于后续研究筛选规则，本轮只确定采集结果的记录方式。

按 `code_hash` 匹配到同一份源码时，共享该源码、AI 静态质检报告及风险等级、已完成的 owner 可读取性分析结论与读取方式、税费接收钱包的源码分析结论、固定地址与读取方式、税率分析结论及读取与解释方案，以及公开资料链接的提取结果与源码出处。依赖链上状态的 owner、税费接收地址及税率按当前合约分别读取。复用高风险报告后，直接对当前项目应用高风险排除规则，并保存该项目的排除记录。其他依赖项目资料的研究结论仍分别形成；复用源码也不将新地址标记为已经在 Etherscan 完成独立源码验证。运行时字节码相同仍可能存在不同的初始化参数和链上状态；Etherscan 的 [相似字节码匹配说明](https://info.etherscan.com/what-is-similar-match-bytecode/) 也区分了代码匹配与部署行为。公开链接必须绑定实际用于提取的源码，复用时保留该源码的来源，不据字节码匹配宣称链接已获当前项目官方确认。源码来源、复用关系与新地址验证状态的具体表达后续设计。

当前实现已有按代码哈希查询和复用源码的机制：`ContractSourceProcessor.Process` 在数据库记录存在且 `Fetched` 为真时直接使用已存资料，并保留原采集时间。当前空源码记录也会被后续请求复用，不会改用当前合约地址重新查询；现有采集任务在第三次失败时结束。这些现状与本次目标中的空源码重新查询、首次请求之外最多重试三次存在差异。实现背景见 [源码采集处理器](../../internal/token/collection/application/processors.go)、[源码缓存读取](../../internal/token/adapters/postgres/collection_context_store.go) 和 [当前采集任务重试](../design/token-intelligence/collection-profile.md)。本轮只整理目标设计，尚未调整代码。

### AI 静态质检、风险过滤与报告复用（待实现）

获取源码的业务目的是由 AI 静态分析生成质检报告、评定风险等级，并据此筛选项目。质检报告判为高风险的项目直接排除，保存风险等级、报告引用及排除依据，不再继续研究，也不交接第二板块。已有明确非高风险结论的项目继续其他研究，不仅凭该报告就通过全部研究筛选或执行买入。图示与报告说明见 [合约源码 AI 静态审阅流程](token-contract-review-flow.md)。

同一份源码只需要一份质检报告，按以下规则获取：

1. 使用当前合约运行时字节码的 `code_hash` 从数据库匹配源码及其质检报告。
2. 已有质检报告时，直接复用报告和风险等级，不因出现新部署地址而重复调用 AI。
3. 尚无质检报告时，取得或复用对应的非空源码，交给 AI 静态审阅，生成报告并评定风险等级，同时识别是否使用代理、是否存在外部未知依赖，保存结论及依据到该源码下，供所有匹配该源码的项目共享。
4. 当前项目引用该报告并应用风险过滤规则；共享报告为高风险时，每个匹配项目都直接排除并分别保存排除记录。

同一源码的质检报告、owner 分析、税费接收钱包分析与税率分析采用一致的共享原则：按 `code_hash` 定位源码，命中已有完成结果直接复用，缺少相应结果时才进行所需分析。共享报告及风险等级、owner 读取方式、税费接收钱包的固定地址与读取方式，以及税率的读取与解释方案，并分别保存各项分析结论及源码依据；依赖链上状态的地址与税率仍由 Go 程序按各合约分别读取。各项分析互不作为完成前提，报告已完成不代表其他分析已完成或一定有可用读取方式。同一源码并发到达时也应避免重复分析，具体去重协调方式后续实现。

公开资料链接也按同一实际源码复用已完成的提取结果，与质检报告及上述读取方式分析分别保存、独立进行，不要求在同一次 AI 调用中完成。源码中的官网或审计报告链接是资料线索，不作为静态审阅已核验项目身份或审计结论的依据。

本环节只分析所提供的源码，不接入静态分析工具，不执行合约，不通过读取链上状态核实 owner、角色、税率、开关或代理实现，也不等待钱包或底池资料。owner、税费接收钱包和税率的 AI 分析、Go 链上读取及后续钱包采集属于独立的研究资料流程，不作为静态质检报告的链上核实步骤。

报告说明审阅范围、发现的问题、源码位置或片段、触发条件、潜在影响，以及风险等级和评级依据。AI 可以指出“源码允许某角色执行危险操作”，但不把它写成已经确认当前链上角色或参数的事实；对疑点或缺少的依赖源码，在报告中说明范围和依据，并给出基于所提供源码的风险等级。报告描述静态代码风险，不核实当前链上状态。

AI 还需基于已取得的源码给出两项独立识别结果，与质检报告一起保存和复用：

| 识别项 | 当前要求 |
| --- | --- |
| 是否使用代理 | 根据源码中的代理调用转发及执行结构判断，保存结论和对应代码依据。 |
| 是否存在外部未知依赖 | 判断是否依赖本次源码中未提供或无法确定具体实现的外部合约逻辑，保存结论、调用位置及未知原因。已完整提供并能在本次源码中审阅的库或父合约，不因导入或继承而自动归为未知依赖。 |

两项结果可以同时成立，结论为“否”仅表示在本次源码范围内未识别到，不代表链上核实其不存在。当前只识别并展示这两项，不新增实际代理实现地址的解析或链上读取，不继续获取、追踪或分析代理实现及外部依赖源码。使用代理或存在外部未知依赖本身不新增自动排除或风险等级提升规则；原有依据实际源码风险生成高风险报告并排除项目的规则继续执行。具体识别边界见 [源码静态审阅流程](token-contract-review-flow.md#代理与外部未知依赖识别)。

AI 请求失败、超时或返回不符合报告要求时属于技术异常，单独记录和处理，不作为业务筛选分支，也不保存为质检报告或据此定级。正常业务流程为取得源码、生成或复用报告及评级、高风险排除或非高风险继续研究。

“高风险直接排除、不再研究”以及“同一源码报告只生成一次并共享复用”已经确定。具体风险等级划分、各类问题如何评级、提示词、报告格式和技术异常处理仍待细化；本轮不新增数值阈值、模型选型或数据结构，也不要求质检报告、owner、税费接收钱包与税率分析在同一次 AI 调用中生成。

### 研究证据与完成条件

高风险质检报告已经足以直接排除项目，不需要等待下述其他资料齐备。以下继续采集与研究的说明适用于尚未被排除的项目；已启动任务的结束处理方式后续设计。

部署交易、部署者、代码哈希、初始接收钱包及分配线索等发现阶段已经取得的信息，应一并作为研究材料。目标设计的 L1 直接关联钱包包括部署者、最多 5 个初始接收钱包、取得的合约 owner 及一个或多个税费接收钱包，四类角色对应的地址均纳入普通交易与代币余额采集。初始接收钱包沿用部署交易内该代币的转账日志，按净接收量从高到低排序，在净接收量为正且查询时无合约代码的地址中最多选取 5 个。该上限仅针对初始接收钱包，不限制税费接收钱包数量；owner 和税费接收钱包也可为合约地址。相同地址保留多个角色标签并去重采集。从 L1 的资金来源发现 L2，再逐层查找 L3、L4、L5，按下述多层流程开展历史研究。

钱包历史交易的查询规则已确认原样保留：对每个去重后的关联钱包，通过 Etherscan 普通交易接口 `account/txlist` 获取项目部署区块之前、距离部署时点最近的最多 300 笔交易。L1–L5 各层钱包均沿用这一规则；300 笔是每个钱包的上限。

| 查询项 | 已确认规则 |
| --- | --- |
| 起始区块 `startblock` | `0` |
| 截止区块 `endblock` | `项目部署区块 - 1`，不包含部署区块内及之后的交易 |
| 排序 `sort` | `desc`，从新到旧 |
| 页码 `page` | `1`，只取第一页，不继续翻页补充更早交易 |
| 每页数量 `offset` | `300`；不足 300 笔时保留实际返回数量，无交易时保留空结果 |

例如项目部署在第 `30000` 区块，则 L1–L5 查询范围均固定为 `0–29999`，倒序取每个钱包最近的最多 300 笔普通交易。即使实际采集时链已推进到更高区块，截止高度仍为 `29999`，也不改为某笔资金转入前的区块。该范围是普通交易的历史样本，不包含内部交易或完整代币转账事件历史；为分析代币交互补充的是这批交易的回执与日志，不扩大 300 笔范围。

这些交易用于整理并展示各层钱包直接部署过的合约、交易或交互过的项目，以及多层资金来源和地址间的交易联系。钱包资料当前只采集、解析和展示，不据此筛选或排除项目。层级范围、资金来源定义和分析内容按下节执行；具体行为解析仍待细化。

当前代码已经实现上述区块范围、排序、页码及数量规则，本轮原样保留。当前钱包范围仍只覆盖部署者和最多 10 个初始接收钱包；初始接收钱包缩减为最多 5 个、owner 与税费接收钱包的获取及纳入采集、L2–L5 来源发现、多层历史分析与资金来源图均待实现。详见 [钱包历史交易现状](../design/token-intelligence/wallet-normal-transactions.md)及对应的[截止区块计算](../../internal/token/collection/application/processors.go)与[Etherscan 查询参数](../../internal/token/adapters/normaltransactions/provider.go)。

现有 `simulation_result` 是六项独立的 `transfer` / `transferFrom` 调用探测，当前仅记录调用是否报错，不是完整的买入、卖出模拟。因此不将现有结果直接作为项目安全、增发能力或买卖可执行性的结论。辅助合约探测与后续买卖可执行性检查的目标和方式仍需分别设计。实现依据见 [当前模拟调用](../../internal/token/adapters/evm/project_state_reader.go)。

研究阶段不再以现有六类采集任务全部结束作为完成或交接的统一前提。本阶段未采集底池信息、未采集 Ave，或没有取得非必采的模拟结果，不应仅因此阻塞研究或将研究结论标为不完整。owner 由 AI 分析源码确定读取方式，再由 Go 程序读取链上结果；税费接收钱包由 AI 提取固定地址或提供方式交由 Go 读取。未取得或只取得部分地址时记录实际结果与原因，保留成功结果并继续使用已有资料研究，不将取得全部 owner 或税费接收地址作为研究完成的必要条件。其他源码、钱包及基础链上信息缺失对研究结论的影响、形成研究结论所需的最小证据，以及同一合约后续验证后的定时补采方式仍待细化；遇到相同哈希但未取得源码时，按前述已明确规则使用当前合约重新查询。这不代表可以跳过必要的项目研究直接买入。

## 已明确的多层关联钱包研究与资金来源图（尚未实现）

当前阶段的职责为“采集钱包数据 → 解析事实与关联关系 → 保存并展示”。多重角色标签、L1–L5 资金来源图、历史部署与交易项目清单、交易依据和 L1 代币余额均用于查看，不设置基于这些钱包数据的风险评级、通过／不通过判断或项目排除逻辑。采集失败、结果为空或解析未完成按实际情况记录并展示，也不转为项目筛选结论；钱包数据驱动的进一步筛选暂不设计。

独立图示见 [多层关联钱包研究流程](token-wallet-research-flow.md)与[钱包资金来源关系图](token-wallet-funding-graph.md)。L1 为部署者、最多 5 个初始接收钱包、已取得的 owner 及全部已取得的税费接收钱包；从 L1 交易样本发现资金来源形成 L2，再依次查找 L3、L4、L5。L5 对应从 L1 向上查找四轮来源；各层统一使用当前项目部署区块之前最近最多 300 笔的窗口，L5 仍开展历史行为分析，但不继续发现 L6。

当前持币地址属于独立分布资料。可按链和地址匹配已知的钱包角色及资金来源关系供展示，但不会仅因持币而自动成为 L1，也不自动触发该地址的 300 笔历史、资金来源扩展或其他资产余额采集。已属于 L1–L5 的地址继续沿用其原有采集范围。

首版资金来源定义为：当前待展开钱包的交易样本内，成功向该钱包直接转入 ETH、BNB 等原生币且金额大于零的发送地址。L1–L4 使用相同规则发现上一层来源，保留来源地址、接收钱包、原生币种类与金额、交易哈希、区块、区块内交易序号和时间，以及钱包角色和样本来源。失败交易不形成实际资金转入依据；USDT 等代币资金来源和内部转账不纳入首版扩展，样本不能代表完整资金来源。

L1–L5 每个钱包均整理两份清单：

| 分析内容 | 需要保存的结果 |
| --- | --- |
| 直接部署过的合约 | 成功的顶层创建交易对应的合约地址、部署交易、区块与时间；进一步识别是否为 Token，匹配已有项目及研究报告，保留引用和依据 |
| 交易或交互过的项目 | 按链和代币合约地址关联项目，保存具体行为、交易哈希、区块与时间；识别有证据的买入、卖出，并与授权、普通转账、被动收款和其他交互分别记录，失败调用按实际结果记录 |

对已选定交易补充回执与日志，并结合调用数据识别代币交互。普通交易的 `to` 可能是路由合约，不能直接当成交互代币；创建出的合约地址可从 Etherscan `contractAddress` 或创建交易回执取得。当前 Token 交易模型尚未保存创建出的合约地址及本流程所需的回执日志，这部分随历史分析实现。

钱包“交易过什么项目”的识别不限定为 PancakeSwap V2 或 Uniswap V2。目标覆盖当前研究链范围内不同 DEX、协议版本、聚合器、通用及自定义路由、多跳与组合交易。采样入口仍为既定的全部 300 笔普通交易，不先按 V2 事件、固定 Router 或少量方法名筛选。先保留有证据的钱包代币关系，再结合对应协议的调用内容与执行日志识别买卖；按链和代币合约地址统一关联项目，不因交易入口不同而拆成不同项目。

授权、普通转账、被动收款和实际买卖分别记录，不单凭 `Transfer`、净收支或顶层方法名推断买卖，也不把路由中间经过的全部代币都归为钱包主动交易过的项目。协议尚未识别时保留原始证据及能够确定的关系，不丢弃交易或据此断言没有交易过项目。具体解析覆盖及必要补充数据后续细化，详见 [交易项目识别的协议覆盖](token-wallet-research-flow.md#交易项目识别的协议覆盖)。这项历史研究范围独立于现有 V2 底池查询和 Swap 索引，不增加研究阶段的底池状态采集。

同一项目内按链和钱包地址去重，共同来源只保存一个节点并分析一次，钱包角色与所有资金关系完整保留。同一钱包可以同时具有“部署者”“owner”“初始接收钱包”“税费接收钱包”等多个角色标签，各标签并存且不互相覆盖，相同标签去重。角色关联具体项目及证据，不能把其他项目中的身份直接套用到当前项目。一个钱包有四个标签仍计为一个钱包，同一历史窗口只采集一份最多 300 笔交易并复用分析，同一数据时点的余额按地址复用；具体示例见 [多重身份与标签](token-wallet-research-flow.md#同一钱包的多重身份与标签)。

来源已在图中时连接已有节点，保留跨层、循环和回流关系，避免重复采集或无限展开。L1 保持为直接关联入口，其他节点按已发现关系中距离任一 L1 的最短上游追溯距离分层；层级与角色标签分别记录，不因多个角色复制钱包或资金连线。发现更短路径时更新层级，复用已有分析并按新层级判断扩展边界。跨项目复用需匹配链、地址、截止区块和采样规则，不能永久复用某钱包的一次历史样本。

展示采用分层资金来源树，底层保存有向关系图。默认先展示 L1、L2，支持展开单个钱包来源、一键展开至 L5，以及折叠分支。折叠只改变显示，已采集的钱包和交易依据仍保留。五层限制深度，当前不增加额外金额门槛或每层来源数量上限；按需展开与折叠控制展示规模，不把未展开或已隐藏的关系当成不存在。

资金连线按链、来源、接收方和资产合并，显示金额、笔数、首次与最近交易时间；点击查看每笔交易依据。同一交易出现在多个钱包样本中时按链和交易哈希去重，样本关联分别保留，汇总不重复计数。点击任一层的钱包可查看其历史部署合约、交互代币及已匹配的项目、研究报告。显示筛选与完整资料分开，连线汇总与其交易明细使用相同范围。

历史关系图中存在连续连线，不自动代表同一笔资金连续流动。例如 L2 在 `25000` 区块向 L1 转账，L3 在 `28000` 区块才向 L2 转账，两笔虽都早于项目部署区块 `30000`，后一笔仍不能解释前一笔的来源。查看某笔入账的上游路径时，按区块与区块内交易序号逐跳核对上游入账早于下游出账；这只筛选已取得的证据，不改变 `B - 1` 和最多 300 笔的查询规则，也不单凭时序判断混合资金中的金额归属。

最终将各层历史项目、代币交互与资金关系关联当前项目，保存交易证据供展示与核对。共同来源本身不直接等同于同一团队；即使关联历史项目已有风险报告，也只展示关联和报告引用，不据此排除当前项目。图上的显示筛选、折叠和路径核对仅影响查看范围，不属于项目筛选。这一资料流程独立于源码 AI 静态质检，自身不执行项目排除或关注项目交接。L1 继续采集已追踪代币余额，L2–L5 仅新增历史交易、行为研究及资金关系。

数据组织参考 GraphSense，节点展开与交易明细参考 MetaSleuth 和 Arkham，Etherscan 多层遍历实现可参考 chainmap。资料链接与适用边界见 [资金来源图参考方案](token-wallet-funding-graph.md#参考方案)。沿用自己的采集和数据库能力承载地址、资金关系及证据，具体技术结构与接口后续设计。

## 已明确的项目研究与筛选目标（待实现）

图示见 [Token 项目研究与筛选流程](token-research-flow.md)，展示研究材料、规则筛选、关注项目交接和被过滤项目的原因保存。

通过 Token 识别后，项目研究的目的是依据研究筛选规则评估项目，保留符合规则的项目，过滤不符合规则的项目，并留下可供后续人工排查的结论、原因和依据。源码质检报告及风险等级按源码共享，生成或命中高风险报告时直接排除当前项目，不再继续研究；明确非高风险的项目继续其他研究。风险评级细则、其余研究方式和筛选规则仍待设计。

| 研究筛选结论 | 后续处理 | 需要保留的信息 |
| --- | --- | --- |
| 符合研究筛选规则 | 作为关注项目交接交易监控与执行板块 | 项目身份、研究结论及支持该结论的依据 |
| 不符合研究筛选规则 | 过滤该项目，不交接交易监控与执行板块 | 项目身份、未通过的具体规则、原因及对应依据，供后续人工排查 |

筛选结论与原因需要关联到对应链和项目持久保存。被过滤项目也应能够查到排除原因，不能只留下“劣质项目”或“不通过”等笼统描述；应说明哪项规则未满足、判断原因是什么、依据哪些已取得的资料。具体存储结构和人工查询入口后续设计。

Token 识别结果与项目研究筛选结论分别表达不同判断：一个项目可以是 Token，但不符合后续项目筛选规则；研究阶段过滤项目不将其重新标记为不是 Token。高风险质检报告直接触发排除已明确；风险评级细则、其余研究规则、阈值、规则组合方式，以及资料缺失或矛盾时如何判定，仍需逐项明确。AI 调用异常不作为项目风险结论，也不将缺失资料直接等同于劣质项目。

## 已明确的合约 owner 与关联钱包研究（尚未实现）

本节属于钱包资料的采集、解析与展示阶段，owner 的取得、缺失及其钱包历史结果均不触发项目筛选或排除。独立的源码 AI 静态质检按前述规则处理。

合约 owner 是项目的关联钱包。取得 owner 地址后，连同部署者、初始接收钱包及税费接收钱包一并纳入普通交易与资产余额采集，并保留各自的角色关联；不同角色可能对应同一地址，也可能对应不同地址。图示见 [合约 owner 获取与关联钱包采集流程](token-owner-wallet-flow.md)。

### AI 分析源码，Go 读取链上 owner

owner 获取统一采用“AI 判断源码是否提供可用读取方式，Go 程序按该方式读取链上数据”的流程。读取方式依据实际源码确定，不限定为 `owner()`、`getowner()` 或其他固定函数名，也不设置预先尝试固定函数的流程。

AI 对每一份源码分析 owner 能否读取，并在可读取时给出 Go 可以执行的读取说明。例如源码提供 `getowner()` 并返回地址，则说明函数签名、所需参数及返回类型，由 Go 程序编码调用、解码结果。分析依据应指向源码中对应的字段或函数，不能只凭函数名称推断其返回值就是 owner。AI 输出读取方式，实际 owner 地址通过当前合约的链上读取取得。

每次获取 owner 时，按以下流程处理：

1. 使用当前合约的运行时字节码 `code_hash` 查询数据库，匹配对应源码已完成的 owner 分析结果。
2. 命中时复用“可读取及读取方式”或“未识别到可用读取方式”的结论，同一份源码不因新部署地址重复分析。
3. 没有已完成结果时，取得或复用对应的非空源码，交给 AI 分析是否可读取 owner 及读取方式。没有源码则记录资料未取得，不能让 AI 无依据分析。
4. 将完成的分析结论、依据及可用读取方式关联源码与 `code_hash` 保存，供相同源码复用。AI 请求失败、超时或无效响应单独记录为技术异常，不保存成“不可读取”的源码结论。
5. 分析或复用结果提供可用方式时，由 Go 程序对当前链、当前合约执行只读查询，以本次采集时最新区块的状态取得 owner；没有可用方式时记录结果，结束本项获取。
6. 保存本次实际使用的方式、读取结果、来源区块与时间。取得地址后纳入 L1；未取得地址则保存原因，继续其他已知钱包与资料采集。

同一份源码共享 owner 分析结论和读取方式；各部署实例的 owner 地址仍分别读取，不能复制其他合约的地址或旧 owner 快照。源码质检报告与 owner 分析分别保存结果，不要求在同一次 AI 调用中取得，也不互相作为完成前提。Go 的 owner 查询不进入静态质检报告的链上核实过程。

### 读取结果与钱包资料

取得 owner 后，沿用关联钱包采集范围：以 `项目部署区块 - 1` 为截止高度，读取部署前最近的最多 300 笔普通交易；按钱包资产采集时最新区块读取 WETH/WBNB、USDT 等已追踪代币余额，不读取原生币余额。owner 为合约地址时也按关联地址采集，不沿用初始接收钱包的无代码地址筛选来排除 owner。地址已在 L1 时追加当前项目的 owner 标签，保留其他角色并复用同一窗口的资料与分析。owner 地址来自当前采集时点，不将其自动解释为部署时的 owner。

没有源码、AI 完成分析但未识别到可用方式，以及 Go 链上调用未取得地址，需要分别保存实际结果与原因。“未识别到读取方式”或“未取得地址”不表示已经确认合约没有 owner，也不能填入部署者等推测地址。无法定位 owner 地址时，只跳过该未知地址的钱包采集；其他已知钱包继续研究，不因此排除项目或改变 Token 识别结论。已被独立源码高风险报告排除的项目不再继续研究。

读取说明的具体结构、Go 支持的读取方式及校验、多种方式或返回值冲突、零地址等特殊结果、AI 与链上请求的重试、分析更新与并发去重、owner 变化后的更新仍待细化。本轮只更新目标设计，不规定数据库结构、模型或新的重试次数。

## 已明确的税费接收钱包获取与研究（尚未实现）

税费接收钱包是项目的 L1 直接关联钱包，可以是一个，也可以是多个。由 AI 分析已取得的合约源码，识别税费去向、对应字段或函数及依据，再取得具体地址。图示见 [税费接收钱包获取流程](token-fee-recipient-wallet-flow.md)。

| 源码中的情况 | 获取方式 |
| --- | --- |
| 可以由源码确定且写死的接收地址 | AI 直接提取地址，保存代码位置及税费接收用途依据。 |
| 接收地址保存在链上状态中，源码提供可用读函数 | AI 提供函数签名、必要参数及返回类型等读取说明，Go 按本次采集时最新区块，对当前链、当前合约执行只读调用，解码出一个或多个地址。 |
| 同时存在固定地址和需读取的地址 | 分别处理并合并结果，保留每个地址的来源；不将两种方式设为整个合约只能选择其一。 |

AI 根据实际税费分配逻辑判断地址用途，不仅凭变量或函数名称识别。支持多个字段、多个读函数或返回多个地址的方式，不只保留第一个接收钱包。读取说明应足够 Go 编码调用与提取地址；需要参数时给出参数类型、值或可确定的取值依据，不猜测参数或执行 AI 生成的任意程序。

可修改变量的初始赋值即使在源码中写成地址字面量，也不能直接当成当前固定接收地址；需要通过可用读函数取得当前值。由部署参数决定但无法从实际源码确定的地址，也不能猜测填写。本项不新增部署初始化资料采集，不解析代理实现地址，也不获取或分析外部依赖源码。

先按 `code_hash` 查询对应源码已完成的税费接收钱包分析结果，命中直接复用，没有时才对非空源码进行 AI 分析。共享识别结论、源码依据、确实固定的地址和读取方式；动态地址由各合约分别读取并保存实际调用、返回值、来源区块与时间，不复制其他部署实例或旧快照。静态质检、owner、税费接收钱包和税率分析分别保存，不要求在同一次 AI 调用中生成，也不互相作为完成前提。

取得的全部地址按链和地址去重加入 L1，追加当前项目的“税费接收钱包”标签，并保留 owner、部署者和初始接收钱包等已有标签。同一地址对应多个税费用途时，保留各用途及其源码或读取依据。“最多 5 个”只限制初始接收钱包，不限制税费接收钱包数量；税费接收钱包可以是合约地址。

后续沿用每个钱包在区块 `0..B-1` 内最近最多 300 笔普通交易、同批回执日志、历史部署与代币交互分析、L2–L5 资金来源发现，以及 L1 已追踪代币余额采集，其中 `B` 为项目部署区块。动态读取得到的是采集时点的配置地址，不自动解释为部署时的接收地址；识别到接收配置也不代表已经发生税费支付。

源码中未发现税费接收钱包、已识别接收逻辑但未找到可用获取方式、未取得源码、AI 技术失败及链上读取失败分别保留实际结果与原因。只取得部分地址时保留成功结果并继续这些钱包的采集，不把未知部分解释为不存在，也不把技术失败保存为可复用的“未发现”结论。本项仅采集、解析和展示，不增加项目风险评级或过滤规则；独立的源码高风险排除规则继续执行。

读取说明的具体结构、Go 支持方式与结果校验、特殊返回值、重试与并发协调、分析更新，以及税费接收地址变更后的更新处理仍待细化。本轮仅补充目标设计。

## 已明确的合约税率读取（尚未实现）

税率沿用“AI 分析源码确定读取方式 → Go 读取当前合约链上数据 → 保存并展示”的思路，作为独立项目资料。完整图示见 [合约税率读取流程](token-tax-rate-flow.md)。税率读取不增加 Token 识别条件，也不改变关联钱包范围。

1. 按当前合约运行时 `code_hash` 查询对应源码已完成的 AI 税率分析结果；已有结果直接复用，没有结果且取得非空源码时才分析。
2. AI 根据实际税费计算逻辑识别可读取的税率及其用途，例如买入税、卖出税、普通转账税或具体分项；不强制每个合约都有这些分类，也不只凭函数名称判断。对每项给出函数签名、必要参数及其确定取值或依据、返回类型与提取位置，以及单位、分母或换算公式的源码依据。
3. Go 按可执行的说明，对当前链、当前合约在本次采集的最新区块进行只读调用，解码原始返回值。同一税率相关分项和动态分母如需多个读函数，固定使用该次采集的区块。
4. 保存税率类型或分项、原始返回值、单位或分母、能够确定的百分比、源码与调用依据，以及实际区块与时间，关联当前项目展示。只有源码明确分项的组合公式时才计算总税率，不将总税率与其分项重复相加。

例如源码明确按 `10000` 为分母扣税，读函数返回 `500`，则展示 `500 / 10000 × 100% = 5%`。分母由源码计算逻辑或对应读函数确定，不统一假定为 `100`、`10000`，也不按代币 `decimals` 推算。单位、分母或组合关系无法确定时，保留原始值与原因，不编造百分比或总税率。

同一份源码共享税率语义、可读取性结论、读取方式和换算依据；实际税率及链上分母逐合约读取，不复用其他部署实例或旧快照。没有源码、未识别到可用读函数、AI 技术失败、链上读取失败与部分成功分别记录。保留成功读取的税率项，未取得的值不填成 `0` 或“无税”；实际读到 `0` 也只表示该项在该时点的返回值为零。技术失败不保存为可复用的“无读取方式”结论，不覆盖有效分析。

本项展示当前读到的税率配置及源码中的适用条件，不承诺某笔买卖的实际扣税结果。本轮不扩展为交易模拟、链上角色或免税名单核实，不解析代理实现或外部依赖，不增加直接读存储或固定函数探测兜底。源码中的固定数值或初始赋值可以作为分析依据，不能冒充本次链上读取结果。

税率资料只采集、解释和展示，不增加阈值、评分或项目过滤规则，也不作为 owner、税费接收钱包或其他资料采集的前置条件。AI 静态质检继续独立生成报告与风险等级，高风险直接排除的规则保持不变，报告不使用税率链上结果进行核实。各项源码分析分别复用，无需在同一次 AI 调用中完成。

调用与换算说明的具体结构、Go 支持与校验、参数或结果冲突、重试与并发协调、分析更新与后续税率刷新仍待细化。本轮仅更新目标设计，尚未实现税率读取。

## 已明确的持币地址信息采集（尚未实现）

研究阶段增加当前持币分布资料，用于查看哪些地址持有目标代币、各自持有多少，以及它们与已知关联钱包的重合情况。该资料依赖链和目标代币地址，不要求先取得源码或完成 AI 分析。图示见 [持币地址信息采集流程](token-holder-flow.md)。本轮保留目标设计，后续开通并配置专用 PRO API Key 后再接入。

| 资料 | 获取与处理方式 |
| --- | --- |
| 当前持币地址及数量 | 使用 Etherscan [`tokenholderlist`](https://docs.etherscan.io/api-reference/endpoint/tokenholderlist)，支持按页获取地址和持有量；保留原始返回值、单位、接口来源、查询时间与分页进度。 |
| 当前持币地址总数 | 使用 [`tokenholdercount`](https://docs.etherscan.io/api-reference/endpoint/tokenholdercount)，单独保存总数和查询时间；地址数量不等同于独立持有人数量。 |
| 持币占比 | 在数量单位明确且有有效总供应量时计算，保留分母来源及采集时点；不使用局部持有人列表的余额之和代替总供应量。数量与供应量不保证来自同一区块，无法可靠换算时保留数量及未计算原因。 |
| 已有角色关联 | 按当前链和地址匹配项目已有的部署者、owner、初始接收钱包、税费接收钱包及资金来源关系，保留已有标签和证据；不凭持币推断身份或共同控制。 |

列表与总数接口当前均要求 Etherscan Standard 或以上的 PRO 套餐。按需获取大额持有人时，可参考 [`topholders`](https://docs.etherscan.io/api-reference/endpoint/topholders) 接口，其最多返回前 1000 个地址；该接口上限不作为分页持有人列表的总量上限，也不要求同时重复采集两份名单。具体分页大小、采集数量策略和刷新频率后续细化。

### 免费与专用 PRO API Key 的分工

用户已明确当前系统内的 Etherscan API Key 均为免费 Key，后续将单独开通一个专门用于持币地址数据的 PRO Key。现有免费 Key 继续用于原有合约源码、钱包普通交易等采集；持币列表、持币地址总数及按需排名接口使用专用 PRO Key。该 Key 不混入普通请求的 Key 轮询范围，也不将持币 PRO 请求回退到免费 Key。

尚未配置专用 PRO Key 时，记录“未配置专用 PRO Key，未采集”，继续其他项目资料采集与展示。权限不足、配额耗尽、限流或请求失败时保存实际原因，不解释为零持币地址或项目异常。实际调用遵守所购套餐、所属账号及对应接口的额度和限频；不因为凭据用途分开就假定平台配额天然独立。

现有 [Etherscan Manager](../design/blockchain-data/etherscan-manager.md)提供源码和普通交易查询及通用 Key 轮询；持币接口与专用凭据选择尚未实现。具体配置字段、请求调度、重试、存储及接口结构后续设计，本轮不修改 Key、配置或服务。

### 结果保存与研究边界

按链、目标代币合约和持有人地址关联并去重，保存各次查询的时间与范围，不按相同 `code_hash` 共享不同代币合约的持仓。当前列表不等于部署交易内的初始分配，也不能代替指定历史区块的完整持有人名单；本次不增加历史持有人重建或初始分配明细采集。

分页只取得部分结果时保留已有地址、数量和未取得原因，明确展示范围，不能标为全量；总数查询与名单采集分别记录结果，不仅凭总数就断言所有分页已经取得。无 Key、请求错误和真实空结果分别表达，不把未采集、权限错误或解析失败填成 `0`。

本项只保存并展示当前持币分布，不增加集中度阈值、评分或过滤规则，也不把全部持币地址自动纳入 L1–L5 历史研究。它记录目标代币的持有量，不扩充 L1 已追踪代币余额的资产配置。未取得持币信息不阻塞其他研究；源码静态质检的高风险排除规则继续独立执行，已被排除的项目不继续研究。

## 已明确的源码公开资料链接提取（尚未实现）

研究阶段增加“从合约源码提取项目公开资料”：由 AI 阅读已经取得的源码，包括注释及字符串中实际出现的链接，重点识别 `https://` 链接，整理并展示一份源码中的多条公开资料。图示见 [源码公开资料链接提取流程](token-public-links-flow.md)。本项只提取源码中的信息，不新增网络搜索、网页抓取、社交数据采集或审计报告内容分析。

| 资料类别 | 识别与展示要求 |
| --- | --- |
| 官网 | 依据源码上下文识别所指的网站，保留原始链接；分类不代表已经核验其官方身份。 |
| X | 收集源码中的 X / Twitter 链接，保留具体账号、帖子等完整路径。 |
| Telegram | 收集群组、频道或其他 Telegram 链接，按实际出现内容保存。 |
| 项目文档 | 收集文档站、白皮书或说明资料链接，不要求与官网域名相同。 |
| GitHub | 收集相关组织、仓库或具体文件等链接，保留具体路径。 |
| 公开审计报告 | 依据源码说明识别报告或审计页面链接；有链接不等于已取得、审阅报告或确认当前合约通过审计。 |
| 其他链接 | 保留无法进一步分类的链接及上下文；依赖库文档、许可证等第三方资料按实际用途标注，不冒充当前项目的官网或项目仓库。 |

### 多链接保存与来源展示

一份源码可以关联多条公开资料，同一类别也可以有多条，不设计成每类仅一个链接。按完整 URL 去重，相同链接在多处出现时合并展示并保留全部出处；同域名下的不同路径、查询参数或片段不能仅凭域名相同就合并。AI 只能提取实际出现的链接，不根据项目名称推测网址，也不将裸域名、账号名或 `http://` 链接自动补写为 HTTPS。其他协议的采集范围后续明确。

每条资料保存完整链接、资料类别、与项目的关系或用途说明、实际源码记录引用，以及文件名、可定位的位置或源码片段等提取依据。统一记录资料来源为“合约源码”，并保留提取时间；该时间不表示链接的发布时间或验证时间。

当这些资料展示在 UI 时，每条链接明确标注 **“来源：合约源码”**，并保留查看源码出处的能力。复用结果时仍关联原提取源码及其出处；不将源码复用描述为当前项目官网已经确认。具体页面布局后续设计，本轮不实现 UI。

### 同源码复用与研究边界

通过 `code_hash` 定位实际源码记录，同一份源码已有完成结果时直接复用，无需因新增项目地址重复调用 AI；缺失结果且已取得源码时才分析。结果绑定实际源码内容及出处，不能仅凭运行时字节码相同，就把另一份不同源码注释中的链接当作本次源码提取结果。

成功分析未发现链接时，保存“源码中未发现公开资料链接”；无源码或技术失败时保留未提取的实际原因，不把请求失败记为已确认没有链接。本项结果不依赖 owner、税率、持币或钱包研究完成，也不作为其他研究资料采集的前提。

公开资料链接只收集、分类和展示，不核验可访问性、官方归属、链接内容或审计结论，不跟随链接扩大代理或外部依赖分析，也不新增项目评分和过滤条件。没有链接不代表项目没有公开资料。既定源码质检高风险排除规则继续独立执行，已排除项目不继续研究。

提示词、输出格式、链接解析细节、去重与并发协调、存储和接口结构仍待细化。本轮仅更新目标设计，尚未实现提取或展示功能。

## 已明确移除的黑名单设计（待删除、尚未实施）

目标设计不再保留系统自有的合约代码黑名单和钱包黑名单，也不将这两类黑名单命中作为项目筛选、关注或买入条件。

| 移除对象 | 当前实现与后续安排 |
| --- | --- |
| 合约代码黑名单 | 当前以合约链上字节码的 `code_hash` 记录黑名单；后续删除该黑名单能力 |
| 钱包黑名单 | 当前以钱包地址记录黑名单；后续删除该黑名单能力 |

后续代码重构时，一并清理两类黑名单对应的数据模型与存储、管理接口、前端页面与导航、接口权限映射和命中检查方法。合约源码、代码哈希及关联钱包仍按前述范围用于项目研究；删除黑名单不扩大其他研究数据的删除范围。

当前两类黑名单的存储和管理能力仍然存在，发现、采集和画像构建流程未调用其命中检查。本轮只记录已确认的移除决定，不删除代码或数据；实际清理时再同步更新当前实现文档。现状入口见 [黑名单模型](../../internal/token/policy/model.go)、[黑名单仓储](../../internal/token/adapters/postgres/blocklist_store.go) 和 [Token 模块访问控制](../design/token-intelligence/access-control.md)。

## 已明确的交易监控覆盖方向（待设计、尚未实现）

第二板块需要扩大底池覆盖范围，支持更多协议、报价资产和底池类型。当前实现只围绕 Ethereum 的 Uniswap V2、BSC 的 PancakeSwap V2，以及目标 Token 与 WETH/WBNB、USDT 组成的交易对采集底池信息；这些现状不作为新设计的固定限制。现状说明见 [ATHENA 聚合查询](../design/token-intelligence/athena-contract.md)。

| 扩展方向 | 目标要求 |
| --- | --- |
| DEX 与协议 | 能够纳入其他 DEX 和交易协议，不将现有两个协议或固定 Factory 作为覆盖边界 |
| 报价资产 | 能够研究和监控与其他报价资产组成的底池，不将 WETH/WBNB、USDT 作为唯一允许的报价资产 |
| 底池类型 | 根据不同协议的池结构和交易机制识别流动性与交易条件，不要求所有协议都符合现有 V2 交易对模型 |
| 同一项目的多个底池 | 允许识别同一项目在不同协议、报价资产和池类型下的多个底池，不预设每个项目只有两个固定底池 |

底池发现、状态监控与交易条件判断由第二板块承担。第一板块继续从 `transaction.To() == nil` 的顶层创建交易发现项目，并按既定范围研究合约与相关钱包，不采集或研究底池。

扩大覆盖是已明确的设计方向，具体接入的链、协议、报价资产与池类型清单、接入优先级、底池识别方式、多池选择及交易执行规则后续确定。文档不将扩展方向表述为已经支持所有链上协议，也不在本轮固定技术方案或修改代码。

## 待确认的设计建议

以下建议来自前期讨论，尚未作为已确定规则。后续需要逐项确认，再纳入正式设计。

| 建议 | 希望解决的问题 | 需要确认的边界 |
| --- | --- | --- |
| 交接后继续研究，并允许更新或撤销关注 | 项目出现新信息后，原有研究结论可能发生变化 | 是否持续研究、何时更新或撤销，以及如何影响第二板块 |
| 第二板块接收项目时立即检查当前交易状态 | 交易可能在研究或交接期间开放 | 接收时如何检查，以及发现已经开放交易后如何处理 |
| 第二板块内部区分监控与执行职责 | 一次买入处理过程中仍需关注其他项目的变化 | 内部职责如何协作；尚未决定组件、进程或并发方式 |
| 将买入结果确认、失败处理和防重复执行纳入执行闭环 | 提交买入请求后，需要确定实际结果并处理不确定状态 | 完成标准、异常恢复方式和重复执行的判断边界 |

## 待进一步明确的业务规则

| 议题 | 后续需要明确的内容 |
| --- | --- |
| 发现范围 | 支持哪些链；开发阶段的创建路径已限定为顶层创建交易，未来是否扩展内部创建另行讨论 |
| 区块扫描细节 | 已明确有进度则续扫、无进度回看三天、逐块处理且每轮最多 100 块、轮末刷新最新高度、追平后等待 1 秒；区块处理失败时每次通知管理员并无限次重试同一区块，`H < C` 时通知管理员人工处理；起点定位方式、单块完成条件、与候选验证和研究的衔接、重试间隔、通知提交失败处理、其余告警细节、人工恢复及其他重组情形的处理后续设计 |
| Token 识别与诊断 | 已明确由聚合合约内部先检查目标地址是否有代码，无代码直接判定不是 Token 并记录原因；有代码后执行现有六项检测，全部通过才为 Token，不独立判断交易是否成功；识别结论及原因返回并关联具体合约持久保存供人工查询；具体返回结构、错误码、存储结构与人工查询入口、检测资源限制及执行异常与区块重试的衔接后续设计 |
| 交易场所范围 | 已明确第二板块需要扩展协议、报价资产和底池类型覆盖；具体清单、接入优先级、底池识别、多池选择及采集范围后续设计 |
| 研究标准与结果记录 | 已明确质检报告评为高风险时直接排除项目，不再研究；其余符合规则的交接、不符合的过滤，并持久保存结论、报告引用、未通过规则、原因及依据供人工排查；风险评级细则、其余规则、信息缺失或矛盾处理，以及存储结构和人工查询入口后续设计 |
| 研究方式 | 按 `code_hash` 匹配源码并复用已完成质检报告，没有报告时由 AI 静态审阅并评级，同时识别是否使用代理、是否存在外部未知依赖并保存依据；同一源码共享上述结果，不核实链上状态，不延伸分析代理实现或外部依赖；报告格式、问题清单、失败处理、并发去重，以及其余研究方式仍待细化 |
| 多层钱包历史研究与资金来源图 | 当前仅采集、解析并展示，不据钱包资料筛选或排除项目。L1 逐层追溯至 L5，各层共用部署前最近最多 300 笔窗口，分析直接部署及跨 DEX、版本、路由的交易和交互项目，L5 不继续发现 L6；按链和地址去重，保留多重角色、共享来源、循环关系与交易证据，按最短距离分层，支持逐层展开、折叠和一键至 L5；协议解析、必要补充数据、回执获取与失败处理、调度与接口、存储与缓存更新仍待细化，钱包筛选规则不纳入当前阶段 |
| 阶段采集与研究完成条件 | 已明确基础信息、字节码、owner、需读取的税费接收地址、税率及钱包资产等链上状态按各自采集时最新区块读取并记录来源区块；同一税率的相关调用使用同一区块。钱包资产不采集原生币余额，保留 WETH/WBNB、USDT 等已追踪代币；本研究阶段不采集或研究底池、模拟结果非必采、不采集 Ave；剩余采集字段、触发与更新方式、最小研究证据和数据缺失处理后续细化 |
| 合约源码复用与补采 | 已明确按字节码哈希复用已有源码；没有可复用源码时，包括此前查询为空的情况，使用当前合约重新查询；请求成功但源码为空标记当前合约未开源，请求失败最多重试三次（本轮按首次之外三次、总计最多四次请求记录），耗尽后通知管理员并将当前合约记为未开源，保留实际失败原因；重试间隔、通知提交失败处理、研究与区块流程衔接、同一合约定时补采、同一哈希并发协调，以及源码来源与地址验证状态的表达后续设计 |
| 合约 owner 数据 | 已明确由 AI 分析每份源码是否可读取 owner 及具体方式，按 `code_hash` 复用已完成的分析结论；可读取时由 Go 按方式读取当前合约最新链上 owner，取得地址后纳入 L1。无源码、无可用方式或读取未取得地址分别记录原因，不据此筛选项目；读取说明及 Go 支持方式、结果校验、特殊返回值、重试、并发与更新机制后续设计 |
| 税费接收钱包数据 | 已明确由 AI 分析源码识别一个或多个税费接收钱包，固定地址直接提取，需要状态读取时由 AI 给出可用方式交由 Go 查询当前合约最新状态，两种来源可同时存在。按 `code_hash` 复用源码结论、固定地址和读取方式，动态结果逐合约保存；地址去重纳入 L1 并保留多重标签，不受初始接收钱包 5 个上限限制。部分结果与失败原因按实际展示，不据此筛选；读取说明、结果校验、特殊值、重试、并发与更新机制后续设计 |
| 合约税率数据 | 已明确由 AI 分析源码中的税率读函数、语义及换算依据，再由 Go 读取当前合约最新状态；按 `code_hash` 复用已完成分析与读取方案，实际数值逐合约读取。保存原始值、单位或分母、可确定的百分比及来源区块，部分或失败不填零；只展示，不增加阈值过滤或实际交易模拟。调用与换算说明、结果校验、重试、并发与更新机制后续设计 |
| 当前持币地址数据与专用 PRO Key | 已明确后续单独开通 PRO Key 用于持币列表、地址总数及按需排名，当前免费 Key 保留原有用途。支持分页、持有量及有依据的占比，保存查询时间、来源、角色匹配与已取得范围；未配置或失败不填零、不阻塞其他研究，不自动纳入关联钱包历史研究，不新增过滤。专用凭据配置与调度、分页策略、限频重试、刷新、缓存与数据结构后续设计 |
| 源码公开资料链接 | 已明确 AI 从源码提取多条公开资料，重点 HTTPS 链接，支持官网、X、Telegram、项目文档、GitHub、公开审计报告等分类；完整 URL 去重保留出处，同一源码复用已完成结果，UI 标注“来源：合约源码”。只提取展示，不抓取或核验外部页面，不新增过滤；提示词、输出格式、解析细节、存储与展示结构后续设计 |
| 交易开放判定 | 添加流动性、存在交易对、开放交易和实际可以买入之间的判定关系 |
| 买入条件 | 研究结论以外还需要检查哪些条件，何时允许执行，以及条件不满足后的处理方式 |
| 研究与开盘的先后关系 | 研究未完成就开放交易，或交接时已经开放交易的项目，是放弃还是允许继续参与 |
| 监控与时效 | 使用哪些变化信息、如何持续检测，以及发现和买入响应的时间目标 |
| 交易执行 | 使用的钱包、执行方式、额度、滑点、手续费、路由、授权与异常处理规则 |
| 买入之后的范围 | 是否包括持仓跟踪、卖出和退出策略，以及这些职责如何组织 |

当前业务主线覆盖到首次买入；持仓和卖出的范围尚未确定。除上述已明确的发现范围与扫描调度、Token 识别条件与逐项诊断要求、阶段采集安排、研究筛选目的与结果记录要求、后续研究需求、黑名单移除决定和交易监控覆盖方向外，本轮不自行设定其他规则阈值、技术选型、数据库结构、接口契约或状态枚举。

## 与当前实现及研究资料的关系

当前 Token 代码主要实现按区块发现项目、创建六类一次性采集任务，并在任务全部结束后尝试构建唯一的不可变画像。该流程提供现状背景，尚未实现本文描述的完整两板块业务主线。前述基础信息与底池采集拆分、AI 确定 owner 与税费接收钱包及税率读取方式、从源码提取公开资料链接、专用 PRO Key 的持币地址采集、不研究底池、模拟结果非必采和不采集 Ave 的目标安排也尚未实现。相关说明见：

- [Token 链扫描与项目初始化](../design/token-intelligence/chain-processor.md)。
- [Token 六源采集与项目画像](../design/token-intelligence/collection-profile.md)。
- [Token 项目查询模型](../design/token-intelligence/project-read-model.md)。

六类数据源、一次性采集、不可变画像以及现有组件边界均不作为新方案的固定约束。后续设计应依据业务要求决定保留、替换或重新组织哪些能力；真正实施代码变更时，再同步更新 [当前系统设计文档](../design/README.md)。

## 第三方发现方案调研备忘（本阶段未采用）

以下记录截至 2026 年 9 月 6 日的官方公开资料，供以后重新评估时参考，不是接入计划或当前开发依赖。价格、权限和数据覆盖可能调整，尚未实际调用接口核验完整性、延迟与账单消耗。

### Bitquery

Bitquery 的 EVM `Calls` 数据可按区块号与合约创建条件查询创建记录，包括内部创建路径，可作为未来扩展发现范围的候选来源。能力说明见 [合约创建查询](https://docs.bitquery.io/docs/blockchain/Ethereum/calls/contract-creation/) 与 [Calls 数据说明](https://docs.bitquery.io/docs/schema/evm/calls/)。

API 积分是调用查询时消耗的额度。根据 [积分规则](https://docs.bitquery.io/docs/ide/points/#how-are-points-calculated-for-the-realtime-dataset)，使用 `dataset: realtime` 时，每次查询每个数据集合（cube）消耗 5 积分，与返回记录数量无关；同一请求涉及多个数据集合时分别计费。只查询一次 `Calls` 按 5 积分计算，不能将所有 HTTP 请求一概视为 5 积分。

| 套餐 | 月付价格（美元） | 每月 API 积分 | 单次只查一个实时数据集合时的查询次数 |
| --- | --- | --- | --- |
| Personal | $49 | 10 万 | 约 2 万次 |
| Pro | $99 | 100 万 | 约 20 万次 |
| Scale | $299 | 500 万 | 约 100 万次 |

例如，1,000 个区块各发起一次仅查询 `Calls` 的实时请求，预计消耗 5,000 积分。分页、重复轮询和额外的数据查询增加用量；历史查询不直接套用这项实时计费规则。积分按付款日期对应的账期刷新，且与每分钟请求数、并发数限制独立。详见 [套餐与账期说明](https://docs.bitquery.io/docs/ide/paid/)。

Personal 仅限个人非商业用途；按区块主动查询可从商用 Pro 评估。若需要 Calls / Traces 的持续推送，则应评估 Scale，其订阅时长和推送数据量单独计量。完整历史数据不包含在基础套餐中，需要核实具体数据集合的历史范围及购买方式。详见 [官方定价](https://bitquery.io/pricing)。

### Allium

Allium 的 [Ethereum contracts 表](https://docs.allium.so/historical-data/supported-blockchains/evm/ethereum/raw/contracts) 来源于 `CREATE` / `CREATE2` 创建记录，包含 `block_number`，可供按区块查询。Explorer 属于历史分析产品；[产品比较](https://docs.allium.so/product-comparison)给出的通用数据新鲜度约为 1 小时，不能直接视为秒级发现新合约的能力，具体合约表延迟和实时产品覆盖仍需确认。

官网列出 [通用付费入口 $200/月起](https://www.allium.so/use-case/product-engineering)，但未承诺该价格包含所需 contracts SQL 权限和额度；[常规生产套餐](https://www.allium.so/compare/allium-vs-zerion)中的 Explorer SQL 与实时数据服务需具体报价。

另有 [Machine Payments 按次付费方式](https://docs.allium.so/ai/machine-payments/endpoints-pricing)：提交 SQL 为 $0.01/次，查询状态为 $0.01/次，获取结果为动态计费。因此 $0.01 不是一次完整查询的总成本；该价格表中状态与结果接口仍要求 API key，具体表权限也需确认，不能只凭提交价格确定可用性和总预算。

返回 [业务设计与研究资料索引](README.md) 或 [文档首页](../index.md)。
