// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: outbox_event.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const claimOutboxEvents = `-- name: ClaimOutboxEvents :many
WITH picked AS (
  SELECT id
  FROM outbox_event
  WHERE status = 'pending'
    AND next_attempt_at <= $1::timestamptz
  ORDER BY id
  LIMIT $3
  FOR UPDATE SKIP LOCKED
)
UPDATE outbox_event e
SET status = 'processing',
  locked_at = $1::timestamptz,
  locked_by = $2,
  attempts = attempts + 1,
  updated_at = now()
FROM picked
WHERE e.id = picked.id
RETURNING e.id, e.type, e.aggregate_type, e.aggregate_id, e.chain_id, e.dedup_key, e.payload, e.status, e.attempts, e.next_attempt_at, e.locked_at, e.locked_by, e.last_error, e.created_at, e.updated_at
`

type ClaimOutboxEventsParams struct {
	Now        pgtype.Timestamptz
	LockedBy   pgtype.Text
	LimitCount int32
}

func (q *Queries) ClaimOutboxEvents(ctx context.Context, arg ClaimOutboxEventsParams) ([]OutboxEvent, error) {
	rows, err := q.db.Query(ctx, claimOutboxEvents, arg.Now, arg.LockedBy, arg.LimitCount)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []OutboxEvent
	for rows.Next() {
		var i OutboxEvent
		if err := rows.Scan(
			&i.ID,
			&i.Type,
			&i.AggregateType,
			&i.AggregateID,
			&i.ChainID,
			&i.DedupKey,
			&i.Payload,
			&i.Status,
			&i.Attempts,
			&i.NextAttemptAt,
			&i.LockedAt,
			&i.LockedBy,
			&i.LastError,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const claimOutboxEventsByTypes = `-- name: ClaimOutboxEventsByTypes :many
WITH picked AS (
  SELECT id
  FROM outbox_event
  WHERE status = 'pending'
    AND type = ANY($3::text[])
    AND next_attempt_at <= $1::timestamptz
  ORDER BY id
  LIMIT $4
  FOR UPDATE SKIP LOCKED
)
UPDATE outbox_event e
SET status = 'processing',
  locked_at = $1::timestamptz,
  locked_by = $2,
  attempts = attempts + 1,
  updated_at = now()
FROM picked
WHERE e.id = picked.id
RETURNING e.id, e.type, e.aggregate_type, e.aggregate_id, e.chain_id, e.dedup_key, e.payload, e.status, e.attempts, e.next_attempt_at, e.locked_at, e.locked_by, e.last_error, e.created_at, e.updated_at
`

type ClaimOutboxEventsByTypesParams struct {
	Now        pgtype.Timestamptz
	LockedBy   pgtype.Text
	Types      []string
	LimitCount int32
}

func (q *Queries) ClaimOutboxEventsByTypes(ctx context.Context, arg ClaimOutboxEventsByTypesParams) ([]OutboxEvent, error) {
	rows, err := q.db.Query(ctx, claimOutboxEventsByTypes,
		arg.Now,
		arg.LockedBy,
		arg.Types,
		arg.LimitCount,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []OutboxEvent
	for rows.Next() {
		var i OutboxEvent
		if err := rows.Scan(
			&i.ID,
			&i.Type,
			&i.AggregateType,
			&i.AggregateID,
			&i.ChainID,
			&i.DedupKey,
			&i.Payload,
			&i.Status,
			&i.Attempts,
			&i.NextAttemptAt,
			&i.LockedAt,
			&i.LockedBy,
			&i.LastError,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const insertOutboxEvent = `-- name: InsertOutboxEvent :one
WITH inserted AS (
  INSERT INTO outbox_event (
    type,
    aggregate_type,
    aggregate_id,
    chain_id,
    dedup_key,
    payload,
    status,
    next_attempt_at
  ) VALUES (
    $1,
    $2,
    $3,
    $4,
    $5,
    $6::jsonb,
    'pending',
    COALESCE($7::timestamptz, now())
  )
  ON CONFLICT (type, chain_id, dedup_key)
    WHERE status IN ('pending', 'processing')
  DO UPDATE SET
    aggregate_type = EXCLUDED.aggregate_type,
    aggregate_id = EXCLUDED.aggregate_id,
    payload = EXCLUDED.payload,
    next_attempt_at = LEAST(outbox_event.next_attempt_at, EXCLUDED.next_attempt_at),
    updated_at = now()
  RETURNING id, type, aggregate_type, aggregate_id, chain_id, dedup_key, payload, status, attempts, next_attempt_at, locked_at, locked_by, last_error, created_at, updated_at
)
SELECT id, type, aggregate_type, aggregate_id, chain_id, dedup_key, payload, status, attempts, next_attempt_at, locked_at, locked_by, last_error, created_at, updated_at
FROM inserted
`

type InsertOutboxEventParams struct {
	Type          string
	AggregateType string
	AggregateID   string
	ChainID       int64
	DedupKey      string
	Payload       []byte
	NextAttemptAt pgtype.Timestamptz
}

type InsertOutboxEventRow struct {
	ID            int64
	Type          string
	AggregateType string
	AggregateID   string
	ChainID       int64
	DedupKey      string
	Payload       []byte
	Status        string
	Attempts      int32
	NextAttemptAt pgtype.Timestamptz
	LockedAt      pgtype.Timestamptz
	LockedBy      pgtype.Text
	LastError     pgtype.Text
	CreatedAt     pgtype.Timestamptz
	UpdatedAt     pgtype.Timestamptz
}

func (q *Queries) InsertOutboxEvent(ctx context.Context, arg InsertOutboxEventParams) (InsertOutboxEventRow, error) {
	row := q.db.QueryRow(ctx, insertOutboxEvent,
		arg.Type,
		arg.AggregateType,
		arg.AggregateID,
		arg.ChainID,
		arg.DedupKey,
		arg.Payload,
		arg.NextAttemptAt,
	)
	var i InsertOutboxEventRow
	err := row.Scan(
		&i.ID,
		&i.Type,
		&i.AggregateType,
		&i.AggregateID,
		&i.ChainID,
		&i.DedupKey,
		&i.Payload,
		&i.Status,
		&i.Attempts,
		&i.NextAttemptAt,
		&i.LockedAt,
		&i.LockedBy,
		&i.LastError,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const markOutboxEventDiscarded = `-- name: MarkOutboxEventDiscarded :exec
UPDATE outbox_event
SET status = 'discarded',
  locked_at = NULL,
  locked_by = NULL,
  last_error = $1,
  updated_at = now()
WHERE id = $2
`

type MarkOutboxEventDiscardedParams struct {
	LastError pgtype.Text
	ID        int64
}

func (q *Queries) MarkOutboxEventDiscarded(ctx context.Context, arg MarkOutboxEventDiscardedParams) error {
	_, err := q.db.Exec(ctx, markOutboxEventDiscarded, arg.LastError, arg.ID)
	return err
}

const markOutboxEventFailed = `-- name: MarkOutboxEventFailed :exec
UPDATE outbox_event
SET status = 'pending',
  locked_at = NULL,
  locked_by = NULL,
  last_error = $1,
  next_attempt_at = $2,
  updated_at = now()
WHERE id = $3
`

type MarkOutboxEventFailedParams struct {
	LastError     pgtype.Text
	NextAttemptAt pgtype.Timestamptz
	ID            int64
}

func (q *Queries) MarkOutboxEventFailed(ctx context.Context, arg MarkOutboxEventFailedParams) error {
	_, err := q.db.Exec(ctx, markOutboxEventFailed, arg.LastError, arg.NextAttemptAt, arg.ID)
	return err
}

const markOutboxEventProcessed = `-- name: MarkOutboxEventProcessed :exec
UPDATE outbox_event
SET status = 'processed',
  locked_at = NULL,
  locked_by = NULL,
  last_error = NULL,
  updated_at = now()
WHERE id = $1
`

func (q *Queries) MarkOutboxEventProcessed(ctx context.Context, id int64) error {
	_, err := q.db.Exec(ctx, markOutboxEventProcessed, id)
	return err
}
