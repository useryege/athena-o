// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: chain_ingest_checkpoint.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getChainIngestCheckpoint = `-- name: GetChainIngestCheckpoint :one
SELECT
  c.id AS chain_id,
  c.name AS chain_name,
  c.enabled,
  COALESCE(cp.cursor_block_number, 0)::bigint AS cursor_block_number,
  COALESCE(cp.status, 'stopped')::text AS status,
  COALESCE(cp.created_at, c.created_at) AS created_at,
  COALESCE(cp.updated_at, c.created_at) AS updated_at
FROM chain c
LEFT JOIN chain_ingest_checkpoint cp ON cp.chain_id = c.id
WHERE c.id = $1
`

type GetChainIngestCheckpointRow struct {
	ChainID           int64
	ChainName         string
	Enabled           bool
	CursorBlockNumber int64
	Status            string
	CreatedAt         pgtype.Timestamptz
	UpdatedAt         pgtype.Timestamptz
}

func (q *Queries) GetChainIngestCheckpoint(ctx context.Context, chainID int64) (GetChainIngestCheckpointRow, error) {
	row := q.db.QueryRow(ctx, getChainIngestCheckpoint, chainID)
	var i GetChainIngestCheckpointRow
	err := row.Scan(
		&i.ChainID,
		&i.ChainName,
		&i.Enabled,
		&i.CursorBlockNumber,
		&i.Status,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listChainIngestCheckpoints = `-- name: ListChainIngestCheckpoints :many
SELECT
  c.id AS chain_id,
  c.name AS chain_name,
  c.enabled,
  COALESCE(cp.cursor_block_number, 0)::bigint AS cursor_block_number,
  COALESCE(cp.status, 'stopped')::text AS status,
  COALESCE(cp.created_at, c.created_at) AS created_at,
  COALESCE(cp.updated_at, c.created_at) AS updated_at
FROM chain c
LEFT JOIN chain_ingest_checkpoint cp ON cp.chain_id = c.id
ORDER BY c.id
`

type ListChainIngestCheckpointsRow struct {
	ChainID           int64
	ChainName         string
	Enabled           bool
	CursorBlockNumber int64
	Status            string
	CreatedAt         pgtype.Timestamptz
	UpdatedAt         pgtype.Timestamptz
}

func (q *Queries) ListChainIngestCheckpoints(ctx context.Context) ([]ListChainIngestCheckpointsRow, error) {
	rows, err := q.db.Query(ctx, listChainIngestCheckpoints)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListChainIngestCheckpointsRow
	for rows.Next() {
		var i ListChainIngestCheckpointsRow
		if err := rows.Scan(
			&i.ChainID,
			&i.ChainName,
			&i.Enabled,
			&i.CursorBlockNumber,
			&i.Status,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateChainIngestCheckpointStatus = `-- name: UpdateChainIngestCheckpointStatus :one
UPDATE chain_ingest_checkpoint
SET status = $1
WHERE chain_id = $2
RETURNING chain_id, cursor_block_number, status, created_at, updated_at
`

type UpdateChainIngestCheckpointStatusParams struct {
	Status  string
	ChainID int64
}

func (q *Queries) UpdateChainIngestCheckpointStatus(ctx context.Context, arg UpdateChainIngestCheckpointStatusParams) (ChainIngestCheckpoint, error) {
	row := q.db.QueryRow(ctx, updateChainIngestCheckpointStatus, arg.Status, arg.ChainID)
	var i ChainIngestCheckpoint
	err := row.Scan(
		&i.ChainID,
		&i.CursorBlockNumber,
		&i.Status,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const upsertChainIngestCheckpoint = `-- name: UpsertChainIngestCheckpoint :one
INSERT INTO chain_ingest_checkpoint (
  chain_id,
  cursor_block_number,
  status
) VALUES (
  $1,
  $2,
  $3
)
ON CONFLICT (chain_id) DO UPDATE
SET cursor_block_number = EXCLUDED.cursor_block_number,
  status = EXCLUDED.status
RETURNING chain_id, cursor_block_number, status, created_at, updated_at
`

type UpsertChainIngestCheckpointParams struct {
	ChainID           int64
	CursorBlockNumber int64
	Status            string
}

func (q *Queries) UpsertChainIngestCheckpoint(ctx context.Context, arg UpsertChainIngestCheckpointParams) (ChainIngestCheckpoint, error) {
	row := q.db.QueryRow(ctx, upsertChainIngestCheckpoint, arg.ChainID, arg.CursorBlockNumber, arg.Status)
	var i ChainIngestCheckpoint
	err := row.Scan(
		&i.ChainID,
		&i.CursorBlockNumber,
		&i.Status,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const upsertChainIngestCheckpointCursor = `-- name: UpsertChainIngestCheckpointCursor :one
INSERT INTO chain_ingest_checkpoint (
  chain_id,
  cursor_block_number,
  status
) VALUES (
  $1,
  $2,
  COALESCE(NULLIF($3::text, ''), 'running')
)
ON CONFLICT (chain_id) DO UPDATE
SET cursor_block_number = EXCLUDED.cursor_block_number
RETURNING chain_id, cursor_block_number, status, created_at, updated_at
`

type UpsertChainIngestCheckpointCursorParams struct {
	ChainID           int64
	CursorBlockNumber int64
	Status            string
}

func (q *Queries) UpsertChainIngestCheckpointCursor(ctx context.Context, arg UpsertChainIngestCheckpointCursorParams) (ChainIngestCheckpoint, error) {
	row := q.db.QueryRow(ctx, upsertChainIngestCheckpointCursor, arg.ChainID, arg.CursorBlockNumber, arg.Status)
	var i ChainIngestCheckpoint
	err := row.Scan(
		&i.ChainID,
		&i.CursorBlockNumber,
		&i.Status,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
