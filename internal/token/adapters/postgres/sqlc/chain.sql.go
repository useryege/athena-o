// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: chain.sql

package sqlc

import (
	"context"
)

const listChains = `-- name: ListChains :many
SELECT id, name, enabled, created_at
FROM chain
ORDER BY id
`

func (q *Queries) ListChains(ctx context.Context) ([]Chain, error) {
	rows, err := q.db.Query(ctx, listChains)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Chain
	for rows.Next() {
		var i Chain
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Enabled,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertChain = `-- name: UpsertChain :one
INSERT INTO chain (id, name, enabled)
VALUES ($1, $2, $3)
ON CONFLICT (id) DO UPDATE SET
  name = EXCLUDED.name,
  enabled = EXCLUDED.enabled
RETURNING id, name, enabled, created_at
`

type UpsertChainParams struct {
	ID      int64
	Name    string
	Enabled bool
}

// Operations read model.
func (q *Queries) UpsertChain(ctx context.Context, arg UpsertChainParams) (Chain, error) {
	row := q.db.QueryRow(ctx, upsertChain, arg.ID, arg.Name, arg.Enabled)
	var i Chain
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Enabled,
		&i.CreatedAt,
	)
	return i, err
}
