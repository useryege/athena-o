// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: system_notifications.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const claimPendingSystemNotificationDeliveries = `-- name: ClaimPendingSystemNotificationDeliveries :many
WITH ready AS (
  SELECT id
  FROM system_notification_deliveries
  WHERE status = 'pending'
    AND attempts < 5
    AND next_attempt_at <= NOW()
    AND (locked_at IS NULL OR locked_at < NOW() - $3::interval)
  ORDER BY created_at ASC, id ASC
  LIMIT $1
  FOR UPDATE SKIP LOCKED
)
UPDATE system_notification_deliveries AS delivery
SET locked_at = NOW(),
    locked_by = $2
FROM ready
WHERE delivery.id = ready.id
RETURNING delivery.id, delivery.source, delivery.severity, COALESCE(delivery.title, '') AS title,
  delivery.body, COALESCE(delivery.link, '') AS link, delivery.channel, delivery.status,
  delivery.telegram_chat, delivery.topic_label, delivery.attempts,
  (SELECT topic.message_thread_id
   FROM system_notification_topics AS topic
   WHERE topic.telegram_chat = delivery.telegram_chat
     AND topic.label = delivery.topic_label) AS message_thread_id
`

type ClaimPendingSystemNotificationDeliveriesParams struct {
	Limit       int32
	LockedBy    pgtype.Text
	LockTimeout pgtype.Interval
}

type ClaimPendingSystemNotificationDeliveriesRow struct {
	ID              int64
	Source          string
	Severity        string
	Title           string
	Body            string
	Link            string
	Channel         string
	Status          string
	TelegramChat    string
	TopicLabel      string
	Attempts        int32
	MessageThreadID int32
}

func (q *Queries) ClaimPendingSystemNotificationDeliveries(ctx context.Context, arg ClaimPendingSystemNotificationDeliveriesParams) ([]ClaimPendingSystemNotificationDeliveriesRow, error) {
	rows, err := q.db.Query(ctx, claimPendingSystemNotificationDeliveries, arg.Limit, arg.LockedBy, arg.LockTimeout)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ClaimPendingSystemNotificationDeliveriesRow
	for rows.Next() {
		var i ClaimPendingSystemNotificationDeliveriesRow
		if err := rows.Scan(
			&i.ID,
			&i.Source,
			&i.Severity,
			&i.Title,
			&i.Body,
			&i.Link,
			&i.Channel,
			&i.Status,
			&i.TelegramChat,
			&i.TopicLabel,
			&i.Attempts,
			&i.MessageThreadID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const countSystemNotificationDeliveries = `-- name: CountSystemNotificationDeliveries :one
SELECT COUNT(*)::bigint
FROM system_notification_deliveries
WHERE ($1::text IS NULL OR status = $1)
  AND ($2::text IS NULL OR severity = $2)
  AND ($3::text IS NULL OR source = $3)
  AND ($4::text IS NULL OR telegram_chat = $4)
  AND ($5::text IS NULL OR topic_label = $5)
  AND (
    $6::text IS NULL
    OR title ILIKE $6
    OR body ILIKE $6
    OR error_message ILIKE $6
    OR provider_message_id ILIKE $6
  )
`

type CountSystemNotificationDeliveriesParams struct {
	Status       pgtype.Text
	Severity     pgtype.Text
	Source       pgtype.Text
	TelegramChat pgtype.Text
	TopicLabel   pgtype.Text
	Keyword      pgtype.Text
}

func (q *Queries) CountSystemNotificationDeliveries(ctx context.Context, arg CountSystemNotificationDeliveriesParams) (int64, error) {
	row := q.db.QueryRow(ctx, countSystemNotificationDeliveries,
		arg.Status,
		arg.Severity,
		arg.Source,
		arg.TelegramChat,
		arg.TopicLabel,
		arg.Keyword,
	)
	var column_1 int64
	err := row.Scan(&column_1)
	return column_1, err
}

const createSystemNotificationDelivery = `-- name: CreateSystemNotificationDelivery :one
INSERT INTO system_notification_deliveries (source, severity, title, body, link, channel, status, telegram_chat, topic_label)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)
RETURNING id, source, severity, COALESCE(title, '') AS title, body, COALESCE(link, '') AS link, channel, status, telegram_chat, topic_label, provider_message_id, error_message, created_at, sent_at,
  (SELECT authorized_at FROM notification_delivery_attempts WHERE notification_delivery_attempts.id = system_notification_deliveries.current_attempt_id) AS authorized_at,
  (SELECT started_at FROM notification_delivery_attempts WHERE notification_delivery_attempts.id = system_notification_deliveries.current_attempt_id) AS started_at,
  (SELECT result_at FROM notification_delivery_attempts WHERE notification_delivery_attempts.id = system_notification_deliveries.current_attempt_id) AS result_at
`

type CreateSystemNotificationDeliveryParams struct {
	Source       string
	Severity     string
	Title        pgtype.Text
	Body         string
	Link         pgtype.Text
	Channel      string
	Status       string
	TelegramChat string
	TopicLabel   string
}

type CreateSystemNotificationDeliveryRow struct {
	ID                int64
	Source            string
	Severity          string
	Title             string
	Body              string
	Link              string
	Channel           string
	Status            string
	TelegramChat      string
	TopicLabel        string
	ProviderMessageID pgtype.Text
	ErrorMessage      pgtype.Text
	CreatedAt         pgtype.Timestamptz
	SentAt            pgtype.Timestamptz
	AuthorizedAt      pgtype.Timestamptz
	StartedAt         pgtype.Timestamptz
	ResultAt          pgtype.Timestamptz
}

func (q *Queries) CreateSystemNotificationDelivery(ctx context.Context, arg CreateSystemNotificationDeliveryParams) (CreateSystemNotificationDeliveryRow, error) {
	row := q.db.QueryRow(ctx, createSystemNotificationDelivery,
		arg.Source,
		arg.Severity,
		arg.Title,
		arg.Body,
		arg.Link,
		arg.Channel,
		arg.Status,
		arg.TelegramChat,
		arg.TopicLabel,
	)
	var i CreateSystemNotificationDeliveryRow
	err := row.Scan(
		&i.ID,
		&i.Source,
		&i.Severity,
		&i.Title,
		&i.Body,
		&i.Link,
		&i.Channel,
		&i.Status,
		&i.TelegramChat,
		&i.TopicLabel,
		&i.ProviderMessageID,
		&i.ErrorMessage,
		&i.CreatedAt,
		&i.SentAt,
		&i.AuthorizedAt,
		&i.StartedAt,
		&i.ResultAt,
	)
	return i, err
}

const createSystemNotificationTopic = `-- name: CreateSystemNotificationTopic :one
INSERT INTO system_notification_topics (telegram_chat, label, message_thread_id)
VALUES ($1, $2, $3)
RETURNING telegram_chat, label, message_thread_id, created_at
`

type CreateSystemNotificationTopicParams struct {
	TelegramChat    string
	Label           string
	MessageThreadID int32
}

func (q *Queries) CreateSystemNotificationTopic(ctx context.Context, arg CreateSystemNotificationTopicParams) (SystemNotificationTopic, error) {
	row := q.db.QueryRow(ctx, createSystemNotificationTopic, arg.TelegramChat, arg.Label, arg.MessageThreadID)
	var i SystemNotificationTopic
	err := row.Scan(
		&i.TelegramChat,
		&i.Label,
		&i.MessageThreadID,
		&i.CreatedAt,
	)
	return i, err
}

const getSystemNotificationDelivery = `-- name: GetSystemNotificationDelivery :one
SELECT id, source, severity, COALESCE(title, '') AS title, body, COALESCE(link, '') AS link,
  channel, status, telegram_chat, topic_label, provider_message_id, error_message, created_at, sent_at,
  (SELECT authorized_at FROM notification_delivery_attempts WHERE notification_delivery_attempts.id = system_notification_deliveries.current_attempt_id) AS authorized_at,
  (SELECT started_at FROM notification_delivery_attempts WHERE notification_delivery_attempts.id = system_notification_deliveries.current_attempt_id) AS started_at,
  (SELECT result_at FROM notification_delivery_attempts WHERE notification_delivery_attempts.id = system_notification_deliveries.current_attempt_id) AS result_at
FROM system_notification_deliveries
WHERE system_notification_deliveries.id = $1
`

type GetSystemNotificationDeliveryRow struct {
	ID                int64
	Source            string
	Severity          string
	Title             string
	Body              string
	Link              string
	Channel           string
	Status            string
	TelegramChat      string
	TopicLabel        string
	ProviderMessageID pgtype.Text
	ErrorMessage      pgtype.Text
	CreatedAt         pgtype.Timestamptz
	SentAt            pgtype.Timestamptz
	AuthorizedAt      pgtype.Timestamptz
	StartedAt         pgtype.Timestamptz
	ResultAt          pgtype.Timestamptz
}

func (q *Queries) GetSystemNotificationDelivery(ctx context.Context, id int64) (GetSystemNotificationDeliveryRow, error) {
	row := q.db.QueryRow(ctx, getSystemNotificationDelivery, id)
	var i GetSystemNotificationDeliveryRow
	err := row.Scan(
		&i.ID,
		&i.Source,
		&i.Severity,
		&i.Title,
		&i.Body,
		&i.Link,
		&i.Channel,
		&i.Status,
		&i.TelegramChat,
		&i.TopicLabel,
		&i.ProviderMessageID,
		&i.ErrorMessage,
		&i.CreatedAt,
		&i.SentAt,
		&i.AuthorizedAt,
		&i.StartedAt,
		&i.ResultAt,
	)
	return i, err
}

const getSystemNotificationDeliveryCounts = `-- name: GetSystemNotificationDeliveryCounts :one
SELECT
  COUNT(*) FILTER (WHERE status = 'pending')::bigint AS pending_count,
  COUNT(*) FILTER (WHERE status = 'pending' AND attempts > 0)::bigint AS retry_count,
  COUNT(*) FILTER (WHERE status = 'failed')::bigint AS failed_count,
  COUNT(*) FILTER (WHERE status = 'sending')::bigint AS sending_count,
  COUNT(*) FILTER (WHERE status = 'unknown')::bigint AS unknown_count
FROM system_notification_deliveries
`

type GetSystemNotificationDeliveryCountsRow struct {
	PendingCount int64
	RetryCount   int64
	FailedCount  int64
	SendingCount int64
	UnknownCount int64
}

func (q *Queries) GetSystemNotificationDeliveryCounts(ctx context.Context) (GetSystemNotificationDeliveryCountsRow, error) {
	row := q.db.QueryRow(ctx, getSystemNotificationDeliveryCounts)
	var i GetSystemNotificationDeliveryCountsRow
	err := row.Scan(
		&i.PendingCount,
		&i.RetryCount,
		&i.FailedCount,
		&i.SendingCount,
		&i.UnknownCount,
	)
	return i, err
}

const getSystemNotificationTopic = `-- name: GetSystemNotificationTopic :one
SELECT telegram_chat, label, message_thread_id, created_at
FROM system_notification_topics
WHERE telegram_chat = $1
  AND label = $2
`

type GetSystemNotificationTopicParams struct {
	TelegramChat string
	Label        string
}

func (q *Queries) GetSystemNotificationTopic(ctx context.Context, arg GetSystemNotificationTopicParams) (SystemNotificationTopic, error) {
	row := q.db.QueryRow(ctx, getSystemNotificationTopic, arg.TelegramChat, arg.Label)
	var i SystemNotificationTopic
	err := row.Scan(
		&i.TelegramChat,
		&i.Label,
		&i.MessageThreadID,
		&i.CreatedAt,
	)
	return i, err
}

const listDispatchSystems = `-- name: ListDispatchSystems :many
SELECT d.id, d.source, d.severity, d.title, d.body, d.link, d.channel, d.status, d.provider_message_id, d.error_message, d.created_at, d.sent_at, d.telegram_chat, d.topic_label, d.attempts, d.next_attempt_at, d.last_attempt_at, d.locked_at, d.locked_by, d.current_attempt_id, t.message_thread_id FROM system_notification_deliveries d
JOIN system_notification_topics t ON t.telegram_chat=d.telegram_chat AND t.label=d.topic_label
WHERE d.status = 'pending' AND d.attempts < 5
ORDER BY d.telegram_chat, d.next_attempt_at, d.id
`

type ListDispatchSystemsRow struct {
	ID                int64
	Source            string
	Severity          string
	Title             pgtype.Text
	Body              string
	Link              pgtype.Text
	Channel           string
	Status            string
	ProviderMessageID pgtype.Text
	ErrorMessage      pgtype.Text
	CreatedAt         pgtype.Timestamptz
	SentAt            pgtype.Timestamptz
	TelegramChat      string
	TopicLabel        string
	Attempts          int32
	NextAttemptAt     pgtype.Timestamptz
	LastAttemptAt     pgtype.Timestamptz
	LockedAt          pgtype.Timestamptz
	LockedBy          pgtype.Text
	CurrentAttemptID  pgtype.UUID
	MessageThreadID   int32
}

func (q *Queries) ListDispatchSystems(ctx context.Context) ([]ListDispatchSystemsRow, error) {
	rows, err := q.db.Query(ctx, listDispatchSystems)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListDispatchSystemsRow
	for rows.Next() {
		var i ListDispatchSystemsRow
		if err := rows.Scan(
			&i.ID,
			&i.Source,
			&i.Severity,
			&i.Title,
			&i.Body,
			&i.Link,
			&i.Channel,
			&i.Status,
			&i.ProviderMessageID,
			&i.ErrorMessage,
			&i.CreatedAt,
			&i.SentAt,
			&i.TelegramChat,
			&i.TopicLabel,
			&i.Attempts,
			&i.NextAttemptAt,
			&i.LastAttemptAt,
			&i.LockedAt,
			&i.LockedBy,
			&i.CurrentAttemptID,
			&i.MessageThreadID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listSystemNotificationDeliveries = `-- name: ListSystemNotificationDeliveries :many
SELECT id, source, severity, COALESCE(title, '') AS title, body, COALESCE(link, '') AS link,
  channel, status, telegram_chat, topic_label, provider_message_id, error_message, created_at, sent_at,
  (SELECT authorized_at FROM notification_delivery_attempts WHERE notification_delivery_attempts.id = system_notification_deliveries.current_attempt_id) AS authorized_at,
  (SELECT started_at FROM notification_delivery_attempts WHERE notification_delivery_attempts.id = system_notification_deliveries.current_attempt_id) AS started_at,
  (SELECT result_at FROM notification_delivery_attempts WHERE notification_delivery_attempts.id = system_notification_deliveries.current_attempt_id) AS result_at
FROM system_notification_deliveries
WHERE ($3::text IS NULL OR status = $3)
  AND ($4::text IS NULL OR severity = $4)
  AND ($5::text IS NULL OR source = $5)
  AND ($6::text IS NULL OR telegram_chat = $6)
  AND ($7::text IS NULL OR topic_label = $7)
  AND (
    $8::text IS NULL
    OR title ILIKE $8
    OR body ILIKE $8
    OR error_message ILIKE $8
    OR provider_message_id ILIKE $8
  )
ORDER BY system_notification_deliveries.created_at DESC, system_notification_deliveries.id DESC
LIMIT $1 OFFSET $2
`

type ListSystemNotificationDeliveriesParams struct {
	Limit        int32
	Offset       int32
	Status       pgtype.Text
	Severity     pgtype.Text
	Source       pgtype.Text
	TelegramChat pgtype.Text
	TopicLabel   pgtype.Text
	Keyword      pgtype.Text
}

type ListSystemNotificationDeliveriesRow struct {
	ID                int64
	Source            string
	Severity          string
	Title             string
	Body              string
	Link              string
	Channel           string
	Status            string
	TelegramChat      string
	TopicLabel        string
	ProviderMessageID pgtype.Text
	ErrorMessage      pgtype.Text
	CreatedAt         pgtype.Timestamptz
	SentAt            pgtype.Timestamptz
	AuthorizedAt      pgtype.Timestamptz
	StartedAt         pgtype.Timestamptz
	ResultAt          pgtype.Timestamptz
}

func (q *Queries) ListSystemNotificationDeliveries(ctx context.Context, arg ListSystemNotificationDeliveriesParams) ([]ListSystemNotificationDeliveriesRow, error) {
	rows, err := q.db.Query(ctx, listSystemNotificationDeliveries,
		arg.Limit,
		arg.Offset,
		arg.Status,
		arg.Severity,
		arg.Source,
		arg.TelegramChat,
		arg.TopicLabel,
		arg.Keyword,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListSystemNotificationDeliveriesRow
	for rows.Next() {
		var i ListSystemNotificationDeliveriesRow
		if err := rows.Scan(
			&i.ID,
			&i.Source,
			&i.Severity,
			&i.Title,
			&i.Body,
			&i.Link,
			&i.Channel,
			&i.Status,
			&i.TelegramChat,
			&i.TopicLabel,
			&i.ProviderMessageID,
			&i.ErrorMessage,
			&i.CreatedAt,
			&i.SentAt,
			&i.AuthorizedAt,
			&i.StartedAt,
			&i.ResultAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const lockSystemNotificationTopic = `-- name: LockSystemNotificationTopic :exec
SELECT pg_advisory_xact_lock(hashtextextended($1 || ':' || $2, 0))
`

type LockSystemNotificationTopicParams struct {
	TelegramChat pgtype.Text
	Label        pgtype.Text
}

func (q *Queries) LockSystemNotificationTopic(ctx context.Context, arg LockSystemNotificationTopicParams) error {
	_, err := q.db.Exec(ctx, lockSystemNotificationTopic, arg.TelegramChat, arg.Label)
	return err
}
