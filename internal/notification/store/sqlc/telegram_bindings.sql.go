// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: telegram_bindings.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const advanceTelegramPollingState = `-- name: AdvanceTelegramPollingState :one
UPDATE telegram_polling_state
SET next_update_id = GREATEST(next_update_id, $1),
    last_update_at = $2,
    updated_at = NOW()
WHERE singleton = TRUE
RETURNING next_update_id, last_poll_at, last_update_at, updated_at
`

type AdvanceTelegramPollingStateParams struct {
	NextUpdateID int64
	LastUpdateAt pgtype.Timestamptz
}

type AdvanceTelegramPollingStateRow struct {
	NextUpdateID int64
	LastPollAt   pgtype.Timestamptz
	LastUpdateAt pgtype.Timestamptz
	UpdatedAt    pgtype.Timestamptz
}

func (q *Queries) AdvanceTelegramPollingState(ctx context.Context, arg AdvanceTelegramPollingStateParams) (AdvanceTelegramPollingStateRow, error) {
	row := q.db.QueryRow(ctx, advanceTelegramPollingState, arg.NextUpdateID, arg.LastUpdateAt)
	var i AdvanceTelegramPollingStateRow
	err := row.Scan(
		&i.NextUpdateID,
		&i.LastPollAt,
		&i.LastUpdateAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteTelegramBinding = `-- name: DeleteTelegramBinding :one
DELETE FROM telegram_bindings
WHERE account_id = $1
RETURNING account_id, telegram_user_id, telegram_chat_id,
  COALESCE(telegram_username, '') AS telegram_username,
  telegram_display_name, status, revision, bound_at, updated_at,
  COALESCE(last_error, '') AS last_error
`

type DeleteTelegramBindingRow struct {
	AccountID           pgtype.UUID
	TelegramUserID      int64
	TelegramChatID      int64
	TelegramUsername    string
	TelegramDisplayName string
	Status              string
	Revision            int64
	BoundAt             pgtype.Timestamptz
	UpdatedAt           pgtype.Timestamptz
	LastError           string
}

func (q *Queries) DeleteTelegramBinding(ctx context.Context, accountID pgtype.UUID) (DeleteTelegramBindingRow, error) {
	row := q.db.QueryRow(ctx, deleteTelegramBinding, accountID)
	var i DeleteTelegramBindingRow
	err := row.Scan(
		&i.AccountID,
		&i.TelegramUserID,
		&i.TelegramChatID,
		&i.TelegramUsername,
		&i.TelegramDisplayName,
		&i.Status,
		&i.Revision,
		&i.BoundAt,
		&i.UpdatedAt,
		&i.LastError,
	)
	return i, err
}

const deleteTelegramBindingAttemptByAccount = `-- name: DeleteTelegramBindingAttemptByAccount :execrows
DELETE FROM telegram_binding_attempts
WHERE account_id = $1
`

func (q *Queries) DeleteTelegramBindingAttemptByAccount(ctx context.Context, accountID pgtype.UUID) (int64, error) {
	result, err := q.db.Exec(ctx, deleteTelegramBindingAttemptByAccount, accountID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const deleteTelegramBindingAttemptByID = `-- name: DeleteTelegramBindingAttemptByID :execrows
DELETE FROM telegram_binding_attempts
WHERE id = $1
`

func (q *Queries) DeleteTelegramBindingAttemptByID(ctx context.Context, id pgtype.UUID) (int64, error) {
	result, err := q.db.Exec(ctx, deleteTelegramBindingAttemptByID, id)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const failTelegramBindingAttempt = `-- name: FailTelegramBindingAttempt :one
UPDATE telegram_binding_attempts
SET status = 'failed',
    failure_reason = $2,
    updated_at = NOW()
WHERE id = $1
RETURNING id, account_id, token_digest, status, failure_reason, expires_at, created_at, updated_at
`

type FailTelegramBindingAttemptParams struct {
	ID            pgtype.UUID
	FailureReason pgtype.Text
}

func (q *Queries) FailTelegramBindingAttempt(ctx context.Context, arg FailTelegramBindingAttemptParams) (TelegramBindingAttempt, error) {
	row := q.db.QueryRow(ctx, failTelegramBindingAttempt, arg.ID, arg.FailureReason)
	var i TelegramBindingAttempt
	err := row.Scan(
		&i.ID,
		&i.AccountID,
		&i.TokenDigest,
		&i.Status,
		&i.FailureReason,
		&i.ExpiresAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getTelegramBinding = `-- name: GetTelegramBinding :one
SELECT account_id, telegram_user_id, telegram_chat_id,
  COALESCE(telegram_username, '') AS telegram_username,
  telegram_display_name, status, revision, bound_at, updated_at,
  COALESCE(last_error, '') AS last_error
FROM telegram_bindings
WHERE account_id = $1
`

type GetTelegramBindingRow struct {
	AccountID           pgtype.UUID
	TelegramUserID      int64
	TelegramChatID      int64
	TelegramUsername    string
	TelegramDisplayName string
	Status              string
	Revision            int64
	BoundAt             pgtype.Timestamptz
	UpdatedAt           pgtype.Timestamptz
	LastError           string
}

func (q *Queries) GetTelegramBinding(ctx context.Context, accountID pgtype.UUID) (GetTelegramBindingRow, error) {
	row := q.db.QueryRow(ctx, getTelegramBinding, accountID)
	var i GetTelegramBindingRow
	err := row.Scan(
		&i.AccountID,
		&i.TelegramUserID,
		&i.TelegramChatID,
		&i.TelegramUsername,
		&i.TelegramDisplayName,
		&i.Status,
		&i.Revision,
		&i.BoundAt,
		&i.UpdatedAt,
		&i.LastError,
	)
	return i, err
}

const getTelegramBindingAttempt = `-- name: GetTelegramBindingAttempt :one
SELECT id, account_id, token_digest, status, failure_reason, expires_at, created_at, updated_at
FROM telegram_binding_attempts
WHERE account_id = $1
`

func (q *Queries) GetTelegramBindingAttempt(ctx context.Context, accountID pgtype.UUID) (TelegramBindingAttempt, error) {
	row := q.db.QueryRow(ctx, getTelegramBindingAttempt, accountID)
	var i TelegramBindingAttempt
	err := row.Scan(
		&i.ID,
		&i.AccountID,
		&i.TokenDigest,
		&i.Status,
		&i.FailureReason,
		&i.ExpiresAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getTelegramBindingAttemptAccountByToken = `-- name: GetTelegramBindingAttemptAccountByToken :one
SELECT account_id
FROM telegram_binding_attempts
WHERE token_digest = $1
`

func (q *Queries) GetTelegramBindingAttemptAccountByToken(ctx context.Context, tokenDigest []byte) (pgtype.UUID, error) {
	row := q.db.QueryRow(ctx, getTelegramBindingAttemptAccountByToken, tokenDigest)
	var account_id pgtype.UUID
	err := row.Scan(&account_id)
	return account_id, err
}

const getTelegramBindingAttemptByTokenForUpdate = `-- name: GetTelegramBindingAttemptByTokenForUpdate :one
SELECT id, account_id, token_digest, status, failure_reason, expires_at, created_at, updated_at
FROM telegram_binding_attempts
WHERE token_digest = $1
FOR UPDATE
`

func (q *Queries) GetTelegramBindingAttemptByTokenForUpdate(ctx context.Context, tokenDigest []byte) (TelegramBindingAttempt, error) {
	row := q.db.QueryRow(ctx, getTelegramBindingAttemptByTokenForUpdate, tokenDigest)
	var i TelegramBindingAttempt
	err := row.Scan(
		&i.ID,
		&i.AccountID,
		&i.TokenDigest,
		&i.Status,
		&i.FailureReason,
		&i.ExpiresAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getTelegramBindingByIdentity = `-- name: GetTelegramBindingByIdentity :one
SELECT account_id, telegram_user_id, telegram_chat_id,
  COALESCE(telegram_username, '') AS telegram_username,
  telegram_display_name, status, revision, bound_at, updated_at,
  COALESCE(last_error, '') AS last_error
FROM telegram_bindings
WHERE telegram_user_id = $1
  AND telegram_chat_id = $2
`

type GetTelegramBindingByIdentityParams struct {
	TelegramUserID int64
	TelegramChatID int64
}

type GetTelegramBindingByIdentityRow struct {
	AccountID           pgtype.UUID
	TelegramUserID      int64
	TelegramChatID      int64
	TelegramUsername    string
	TelegramDisplayName string
	Status              string
	Revision            int64
	BoundAt             pgtype.Timestamptz
	UpdatedAt           pgtype.Timestamptz
	LastError           string
}

func (q *Queries) GetTelegramBindingByIdentity(ctx context.Context, arg GetTelegramBindingByIdentityParams) (GetTelegramBindingByIdentityRow, error) {
	row := q.db.QueryRow(ctx, getTelegramBindingByIdentity, arg.TelegramUserID, arg.TelegramChatID)
	var i GetTelegramBindingByIdentityRow
	err := row.Scan(
		&i.AccountID,
		&i.TelegramUserID,
		&i.TelegramChatID,
		&i.TelegramUsername,
		&i.TelegramDisplayName,
		&i.Status,
		&i.Revision,
		&i.BoundAt,
		&i.UpdatedAt,
		&i.LastError,
	)
	return i, err
}

const getTelegramBindingForShare = `-- name: GetTelegramBindingForShare :one
SELECT account_id, telegram_user_id, telegram_chat_id,
  COALESCE(telegram_username, '') AS telegram_username,
  telegram_display_name, status, revision, bound_at, updated_at,
  COALESCE(last_error, '') AS last_error
FROM telegram_bindings
WHERE account_id = $1
FOR SHARE
`

type GetTelegramBindingForShareRow struct {
	AccountID           pgtype.UUID
	TelegramUserID      int64
	TelegramChatID      int64
	TelegramUsername    string
	TelegramDisplayName string
	Status              string
	Revision            int64
	BoundAt             pgtype.Timestamptz
	UpdatedAt           pgtype.Timestamptz
	LastError           string
}

func (q *Queries) GetTelegramBindingForShare(ctx context.Context, accountID pgtype.UUID) (GetTelegramBindingForShareRow, error) {
	row := q.db.QueryRow(ctx, getTelegramBindingForShare, accountID)
	var i GetTelegramBindingForShareRow
	err := row.Scan(
		&i.AccountID,
		&i.TelegramUserID,
		&i.TelegramChatID,
		&i.TelegramUsername,
		&i.TelegramDisplayName,
		&i.Status,
		&i.Revision,
		&i.BoundAt,
		&i.UpdatedAt,
		&i.LastError,
	)
	return i, err
}

const getTelegramPollingState = `-- name: GetTelegramPollingState :one
SELECT next_update_id, last_poll_at, last_update_at, updated_at
FROM telegram_polling_state
WHERE singleton = TRUE
`

type GetTelegramPollingStateRow struct {
	NextUpdateID int64
	LastPollAt   pgtype.Timestamptz
	LastUpdateAt pgtype.Timestamptz
	UpdatedAt    pgtype.Timestamptz
}

func (q *Queries) GetTelegramPollingState(ctx context.Context) (GetTelegramPollingStateRow, error) {
	row := q.db.QueryRow(ctx, getTelegramPollingState)
	var i GetTelegramPollingStateRow
	err := row.Scan(
		&i.NextUpdateID,
		&i.LastPollAt,
		&i.LastUpdateAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listTelegramBindingsByIdentityForShare = `-- name: ListTelegramBindingsByIdentityForShare :many
SELECT account_id, telegram_user_id, telegram_chat_id,
  COALESCE(telegram_username, '') AS telegram_username,
  telegram_display_name, status, revision, bound_at, updated_at,
  COALESCE(last_error, '') AS last_error
FROM telegram_bindings
WHERE telegram_user_id = $1
   OR telegram_chat_id = $2
FOR SHARE
`

type ListTelegramBindingsByIdentityForShareParams struct {
	TelegramUserID int64
	TelegramChatID int64
}

type ListTelegramBindingsByIdentityForShareRow struct {
	AccountID           pgtype.UUID
	TelegramUserID      int64
	TelegramChatID      int64
	TelegramUsername    string
	TelegramDisplayName string
	Status              string
	Revision            int64
	BoundAt             pgtype.Timestamptz
	UpdatedAt           pgtype.Timestamptz
	LastError           string
}

func (q *Queries) ListTelegramBindingsByIdentityForShare(ctx context.Context, arg ListTelegramBindingsByIdentityForShareParams) ([]ListTelegramBindingsByIdentityForShareRow, error) {
	rows, err := q.db.Query(ctx, listTelegramBindingsByIdentityForShare, arg.TelegramUserID, arg.TelegramChatID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListTelegramBindingsByIdentityForShareRow
	for rows.Next() {
		var i ListTelegramBindingsByIdentityForShareRow
		if err := rows.Scan(
			&i.AccountID,
			&i.TelegramUserID,
			&i.TelegramChatID,
			&i.TelegramUsername,
			&i.TelegramDisplayName,
			&i.Status,
			&i.Revision,
			&i.BoundAt,
			&i.UpdatedAt,
			&i.LastError,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const lockTelegramBindingAccount = `-- name: LockTelegramBindingAccount :exec
SELECT pg_advisory_xact_lock(hashtextextended('athena:account:' || $1::uuid::text, 0))
`

func (q *Queries) LockTelegramBindingAccount(ctx context.Context, accountID pgtype.UUID) error {
	_, err := q.db.Exec(ctx, lockTelegramBindingAccount, accountID)
	return err
}

const lockTelegramBindingIdentity = `-- name: LockTelegramBindingIdentity :exec
SELECT pg_advisory_xact_lock(
  hashtextextended(
    'telegram-binding:' || $1::bigint::text || ':' || $2::bigint::text,
    0
  )
)
`

type LockTelegramBindingIdentityParams struct {
	TelegramUserID int64
	TelegramChatID int64
}

func (q *Queries) LockTelegramBindingIdentity(ctx context.Context, arg LockTelegramBindingIdentityParams) error {
	_, err := q.db.Exec(ctx, lockTelegramBindingIdentity, arg.TelegramUserID, arg.TelegramChatID)
	return err
}

const markTelegramBindingConnectedByIdentity = `-- name: MarkTelegramBindingConnectedByIdentity :execrows
UPDATE telegram_bindings
SET status = 'connected',
    updated_at = NOW(),
    last_error = NULL
WHERE telegram_user_id = $1
  AND telegram_chat_id = $2
  AND status <> 'connected'
`

type MarkTelegramBindingConnectedByIdentityParams struct {
	TelegramUserID int64
	TelegramChatID int64
}

func (q *Queries) MarkTelegramBindingConnectedByIdentity(ctx context.Context, arg MarkTelegramBindingConnectedByIdentityParams) (int64, error) {
	result, err := q.db.Exec(ctx, markTelegramBindingConnectedByIdentity, arg.TelegramUserID, arg.TelegramChatID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const markTelegramBindingUnreachable = `-- name: MarkTelegramBindingUnreachable :execrows
UPDATE telegram_bindings
SET status = 'unreachable',
    updated_at = NOW(),
    last_error = $4
WHERE account_id = $1
  AND telegram_chat_id = $2
  AND revision = $3
  AND status <> 'unreachable'
`

type MarkTelegramBindingUnreachableParams struct {
	AccountID      pgtype.UUID
	TelegramChatID int64
	Revision       int64
	LastError      pgtype.Text
}

func (q *Queries) MarkTelegramBindingUnreachable(ctx context.Context, arg MarkTelegramBindingUnreachableParams) (int64, error) {
	result, err := q.db.Exec(ctx, markTelegramBindingUnreachable,
		arg.AccountID,
		arg.TelegramChatID,
		arg.Revision,
		arg.LastError,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const markTelegramBindingUnreachableByIdentity = `-- name: MarkTelegramBindingUnreachableByIdentity :one
UPDATE telegram_bindings
SET status = 'unreachable',
    updated_at = NOW(),
    last_error = $3
WHERE telegram_user_id = $1
  AND telegram_chat_id = $2
  AND status <> 'unreachable'
RETURNING account_id, telegram_chat_id, revision
`

type MarkTelegramBindingUnreachableByIdentityParams struct {
	TelegramUserID int64
	TelegramChatID int64
	LastError      pgtype.Text
}

type MarkTelegramBindingUnreachableByIdentityRow struct {
	AccountID      pgtype.UUID
	TelegramChatID int64
	Revision       int64
}

func (q *Queries) MarkTelegramBindingUnreachableByIdentity(ctx context.Context, arg MarkTelegramBindingUnreachableByIdentityParams) (MarkTelegramBindingUnreachableByIdentityRow, error) {
	row := q.db.QueryRow(ctx, markTelegramBindingUnreachableByIdentity, arg.TelegramUserID, arg.TelegramChatID, arg.LastError)
	var i MarkTelegramBindingUnreachableByIdentityRow
	err := row.Scan(&i.AccountID, &i.TelegramChatID, &i.Revision)
	return i, err
}

const nextTelegramBindingRevision = `-- name: NextTelegramBindingRevision :one
INSERT INTO telegram_binding_versions (account_id, revision)
VALUES ($1, 1)
ON CONFLICT (account_id) DO UPDATE
SET revision = telegram_binding_versions.revision + 1
RETURNING revision
`

func (q *Queries) NextTelegramBindingRevision(ctx context.Context, accountID pgtype.UUID) (int64, error) {
	row := q.db.QueryRow(ctx, nextTelegramBindingRevision, accountID)
	var revision int64
	err := row.Scan(&revision)
	return revision, err
}

const recordTelegramPoll = `-- name: RecordTelegramPoll :one
UPDATE telegram_polling_state
SET last_poll_at = $1,
    updated_at = NOW()
WHERE singleton = TRUE
RETURNING next_update_id, last_poll_at, last_update_at, updated_at
`

type RecordTelegramPollRow struct {
	NextUpdateID int64
	LastPollAt   pgtype.Timestamptz
	LastUpdateAt pgtype.Timestamptz
	UpdatedAt    pgtype.Timestamptz
}

func (q *Queries) RecordTelegramPoll(ctx context.Context, lastPollAt pgtype.Timestamptz) (RecordTelegramPollRow, error) {
	row := q.db.QueryRow(ctx, recordTelegramPoll, lastPollAt)
	var i RecordTelegramPollRow
	err := row.Scan(
		&i.NextUpdateID,
		&i.LastPollAt,
		&i.LastUpdateAt,
		&i.UpdatedAt,
	)
	return i, err
}

const replaceTelegramBinding = `-- name: ReplaceTelegramBinding :one
INSERT INTO telegram_bindings (
  account_id, telegram_user_id, telegram_chat_id, telegram_username,
  telegram_display_name, status, revision
)
VALUES ($1, $2, $3, $4, $5, 'connected', $6)
ON CONFLICT (account_id) DO UPDATE
SET telegram_user_id = EXCLUDED.telegram_user_id,
    telegram_chat_id = EXCLUDED.telegram_chat_id,
    telegram_username = EXCLUDED.telegram_username,
    telegram_display_name = EXCLUDED.telegram_display_name,
    status = 'connected',
    revision = EXCLUDED.revision,
    bound_at = NOW(),
    updated_at = NOW(),
    last_error = NULL
RETURNING account_id, telegram_user_id, telegram_chat_id,
  COALESCE(telegram_username, '') AS telegram_username,
  telegram_display_name, status, revision, bound_at, updated_at,
  COALESCE(last_error, '') AS last_error
`

type ReplaceTelegramBindingParams struct {
	AccountID           pgtype.UUID
	TelegramUserID      int64
	TelegramChatID      int64
	TelegramUsername    pgtype.Text
	TelegramDisplayName string
	Revision            int64
}

type ReplaceTelegramBindingRow struct {
	AccountID           pgtype.UUID
	TelegramUserID      int64
	TelegramChatID      int64
	TelegramUsername    string
	TelegramDisplayName string
	Status              string
	Revision            int64
	BoundAt             pgtype.Timestamptz
	UpdatedAt           pgtype.Timestamptz
	LastError           string
}

func (q *Queries) ReplaceTelegramBinding(ctx context.Context, arg ReplaceTelegramBindingParams) (ReplaceTelegramBindingRow, error) {
	row := q.db.QueryRow(ctx, replaceTelegramBinding,
		arg.AccountID,
		arg.TelegramUserID,
		arg.TelegramChatID,
		arg.TelegramUsername,
		arg.TelegramDisplayName,
		arg.Revision,
	)
	var i ReplaceTelegramBindingRow
	err := row.Scan(
		&i.AccountID,
		&i.TelegramUserID,
		&i.TelegramChatID,
		&i.TelegramUsername,
		&i.TelegramDisplayName,
		&i.Status,
		&i.Revision,
		&i.BoundAt,
		&i.UpdatedAt,
		&i.LastError,
	)
	return i, err
}

const upsertTelegramBindingAttempt = `-- name: UpsertTelegramBindingAttempt :one
INSERT INTO telegram_binding_attempts (
  id, account_id, token_digest, status, failure_reason, expires_at
)
VALUES ($1, $2, $3, 'pending', NULL, $4)
ON CONFLICT (account_id) DO UPDATE
SET id = EXCLUDED.id,
    token_digest = EXCLUDED.token_digest,
    status = 'pending',
    failure_reason = NULL,
    expires_at = EXCLUDED.expires_at,
    created_at = NOW(),
    updated_at = NOW()
RETURNING id, account_id, token_digest, status, failure_reason, expires_at, created_at, updated_at
`

type UpsertTelegramBindingAttemptParams struct {
	ID          pgtype.UUID
	AccountID   pgtype.UUID
	TokenDigest []byte
	ExpiresAt   pgtype.Timestamptz
}

func (q *Queries) UpsertTelegramBindingAttempt(ctx context.Context, arg UpsertTelegramBindingAttemptParams) (TelegramBindingAttempt, error) {
	row := q.db.QueryRow(ctx, upsertTelegramBindingAttempt,
		arg.ID,
		arg.AccountID,
		arg.TokenDigest,
		arg.ExpiresAt,
	)
	var i TelegramBindingAttempt
	err := row.Scan(
		&i.ID,
		&i.AccountID,
		&i.TokenDigest,
		&i.Status,
		&i.FailureReason,
		&i.ExpiresAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
