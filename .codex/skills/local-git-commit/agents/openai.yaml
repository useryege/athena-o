interface:
  display_name: "Local Git Commit"
  short_description: "Review changes and create a detailed local commit"
  default_prompt: "Use $local-git-commit to review current changes and create a detailed local Git commit."
